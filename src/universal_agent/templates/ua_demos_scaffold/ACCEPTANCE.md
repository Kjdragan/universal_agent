# Acceptance Contract

> **Status:** placeholder — Simone fills this in during Phase 2 (briefing).
>
> Cody MUST satisfy every numbered requirement before marking the demo
> complete. Simone re-runs the acceptance check in Phase 4 (review).

## Requirements

1. <Cody MUST do X>
2. <Cody MUST NOT do Y>
3. <Demo MUST run end-to-end via `uv run` (or appropriate runner)>
4. <Success stdout pattern: contains the string "...">

## Anti-patterns

- <Patterns Cody should avoid>

## must_use_examples

- pattern: <name of pattern>
  reference: SOURCES/<doc>.md#L<line-range>

## endpoint_required

`anthropic_native` — see `manifest.json` after the run for what was actually hit.

## min_versions

(Filled in from the vault entity page frontmatter. Demo will not run if VPS
versions are below these.)
