{
  "objective": "Search for the latest Russia–Ukraine war developments from the last week. Create a comprehensive report, save it as HTML + PDF, and email it to kevin.dragan@outlook.com. This run is used to test durability (kill/resume) and idempotency without duplicate side effects.",
  "inputs": {
    "report": {
      "title": "Russia-Ukraine War Weekly Report",
      "format": [
        "html",
        "pdf"
      ],
      "notes": "This job is intentionally stable and repeatable. Use it to validate kill/resume and side-effect deduplication."
    },
    "delivery": {
      "email_to": [
        "kevin.dragan@outlook.com"
      ],
      "email_subject": "[Durability Test] Russia-Ukraine War Report (send once; safe resume)",
      "email_body": "Attached is the report generated by the agent. This run is used to test resume + idempotency under forced restarts. If you receive duplicates, the durability guarantees are broken."
    },
    "progress_notifications": {
      "enabled": true,
      "channel": "stdout_or_telegram",
      "events": [
        "crawl_complete",
        "report_complete",
        "pdf_render_complete",
        "upload_complete",
        "email_sent",
        "run_done"
      ]
    }
  },
  "constraints": {
    "must_cite_sources": true,
    "no_duplicate_external_actions": true,
    "no_duplicate_memory_mutations": true
  },
  "tasks": [
    "Use the normal complex-query workflow (Composio search + report-creation-expert + finalize_research) instead of ad-hoc crawling.",
    "If interrupted, resume without duplicating side effects (email/upload).",
    "Prefer filtered crawl corpus for report content; use search JSON snippets for coverage."
  ],
  "budgets": {
    "max_wallclock_minutes": 90,
    "max_steps": 60,
    "max_tool_calls": 400,
    "max_retries_per_tool_call": 5,
    "max_same_error_repeats": 3
  },
  "stop_conditions": [
    "report_html_created",
    "report_pdf_created",
    "upload_succeeded_once",
    "email_sent_once",
    "final_notification_sent"
  ],
  "artifacts": {
    "expected_outputs": [
      "report.html",
      "report.pdf",
      "transcript.md",
      "trace.json"
    ],
    "notes": "Artifacts should be written to the run workspace and referenced in the runtime DB."
  }
}
