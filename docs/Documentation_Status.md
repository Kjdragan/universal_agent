**Last updated:** 2026-05-29 (PM) — Canonical DB path registry + CSI split-brain hardening. **Doc (UPDATED):** `docs/01_Architecture/Database_Architecture.md` gains a new **§0 "Canonical DB Paths & Resolvers — READ FIRST"**: a resolver registry (`_csi_default_db_path()` → `/var/lib/universal-agent/csi/csi.db`; `get_runtime_db_path()`; `get_activity_db_path()`; VP DBs; watchlist JSON) with env overrides + default paths, the runtime_state-vs-activity_state task-population distinction, and a **stale/relic blocklist** (the pre-2026-05-20 split-brain copy `CSI_Ingester/development/var/csi.db` that froze at the exact moment the `CSI_DB_PATH` override landed). Three relic DBs deleted from the VPS (`csi.db`, `csi_events.db`, `csi_ingester.db`). Motivation: two consecutive investigation sessions reached a false "CSI pipeline is dead" conclusion by querying the stale dev `csi.db` instead of resolving the canonical path; this section + the deletion close that footgun. Companion: `docs/02_Subsystems/CSI_YouTube_Flow_2026-05-29.md` (regenerated v2) and `docs/proactive_signals/youtube_feed_topology_handoff_2026-05-29.md`.

**Last updated:** 2026-05-29 — YouTube/CSI feed topology handoff doc added (`docs/proactive_signals/youtube_feed_topology_handoff_2026-05-29.md`, indexed in `docs/README.md` § Proactive Signals & Intelligence Pipeline). Code-verified cross-session investigation handoff mapping the **two distinct YouTube feeds** that read one shared `channels_watchlist.json` (443 channels; tiers gold=22/sidecar=417/blocked=4): **Feed 1 = gold-only Daily YouTube Digest** (`youtube_gold_channel_poller` 5:30 AM → day playlists cap 10 → `youtube_daily_digest` 6:00 AM → one synthesized digest; bounded, healthy) and **Feed 2 = full-watchlist Convergence/Ideation** (CSI `youtube_channel_rss` hourly over all 444 channels → `sync_topic_signatures_from_csi` → Convergence Detection + Ideation Sweep → `convergence_candidate` → Atlas; broad/hourly, source of the parked+cancelled backlog). Proposes retiring "Track A/B" → **Convergence Detection** (cross-channel signal reinforcement / salience confirmation — valuable, less revelatory) vs **Ideation Sweep** (cross-cutting synthesis, more revelatory); documents the deprecated `insight_detection` legacy firehose (0.14% completion, PR-E cleanup pending) and the `task_hub.db`↔`activity_state.db` split-brain. Terminology not yet ratified. Handoff artifact (not yet a canonical reference).

**Last updated:** 2026-05-28 (PM) — gws Gmail fallback labeling LIVE-VERIFIED on the VPS + headless-auth runbook (PR `worktree-claude-gws-vps-runbook`). Followed the operator to verify Phase 1 end-to-end and document the provisioning so it doesn't get re-derived. **Live verification:** desktop drove the real `_send_via_gmail_cli` → Gmail msg `19e70d088c666cb4` in SENT with `UA/AgentSent/Simone`; VPS (`ua`, prod Infisical creds) → msg `19e70e486fe5b117` same result. **Root-caused the recurring `invalid_grant`:** the gws OAuth app is in Google "Testing" mode (refresh tokens die ~7 days) — durable fix is publishing it to "In production" (Kevin/Google-Console, one-time). **Mechanism documented (was undocumented tribal knowledge):** VPS gws creds are supplied via four Infisical `production` secrets `GWS_{CREDENTIALS_ENC,TOKEN_CACHE,ENCRYPTION_KEY,CLIENT_SECRET_JSON}_B64`, materialized into `/home/ua/.config/gws/` by `discord_intelligence/calendar_sync.py` with `KEYRING_BACKEND=file`; gateway + discord both run as `ua` (shared HOME). Refreshed all four secrets with freshly-authed desktop creds (SHA-256 round-trip verified). **Code hardening:** `_send_via_gmail_cli` now `setdefault`s `GOOGLE_WORKSPACE_CLI_KEYRING_BACKEND=file` in its subprocess env so the headless gateway fallback is self-sufficient (no dependency on another service exporting it); 2 new tests (default applied / explicit value preserved); file now 13 tests, all green; ruff clean. **Docs:** new `CLAUDE.md` § "gws (Google Workspace CLI) Auth on the VPS — RUNBOOK" (refresh procedure, `KEY=@file` NOT supported gotcha, empty-`GOOGLE_WORKSPACE_CLI_*` footgun, headless `KEYRING_BACKEND=file` verification trick, 7-day root cause); `docs/03_Operations/82_…` § Gmail (gws) CLI Fallback verification flipped to LIVE-VERIFIED + new "gws auth provisioning on the VPS (headless)" subsection. Earlier 2026-05-28 (PM) entry: Doc reconciliation: stale "Opus 4.7" current-model pins updated to Opus 4.8 in `docs/06_Deployment_And_Environments/09_Demo_Execution_Environments.md` (lines 27, 261) and `10_Interactive_Coding_Environment.md` (lines 30, 437, 446). Follow-up to the #544 routing audit, which flagged these docs as version-stale. Doc 10's dated 2026-05-08 verification banner (`Opus 4.7 · Claude Max`) intentionally left intact — it's a timestamped historical observation, not a current-state pin. Docs-only; no code or behavior change. Earlier entry: 2026-05-28 (PM) — Gmail Sent-copy labeling for the gws fallback (Phase 1, PR `worktree-gmail-fallback-label`). A 429→gws fallback send lands in Kevin's Gmail Sent folder, not AgentMail's Sent box, and AgentMail has no API to import a sent message (`messages.send` is the only creation path and is exactly what 429'd; `messages.update` only toggles labels). So instead of faking an AgentMail record, the real Gmail copy is now made self-identifying: after a successful `+send`, `agentmail_service._apply_gmail_label` stamps it with a nested `UA/AgentSent/<principal>` label (principal defaults `Simone`; resolved/created via `gws gmail users labels list|create`, applied via `… messages modify`, label id cached on the instance). Best-effort — every label failure is logged at WARNING and swallowed so it can never break an already-sent email; reuses the send's scrubbed `child_env`. Gated by `UA_AGENTMAIL_GMAIL_LABEL=1` (default on). Success return dict gains a `"label"` key. Makes "what did Simone send via the Gmail fallback" a one-filter Gmail search (`label:UA/AgentSent/Simone in:sent`) for natural-language debugging. Forward-compat: `principal` arg + scheme ready for a Phase 2 VP-path fallback (Atlas/Cody) — **not built**; this service is Simone-only and Atlas/Cody send via the in-session AgentMail MCP tool (`vp.agents@agentmail.to`) which does not route through this Python fallback. Phase 2 is justified by a VPS log investigation finding 316 `Daily send limit exceeded` 429s against the VP mailbox (93% recent) but is a separate, larger change. Tests: 3 new in `test_agentmail_gmail_fallback.py` (create-then-apply, reuse-existing-label, label-failure-non-fatal); file now 11 tests, all green; ruff clean. **Not yet live-verified** — desktop+VPS `gws auth status` report `token_valid: false` (`invalid_grant`); needs an interactive `gws auth login` before the label calls run end-to-end. Canonical doc: `docs/03_Operations/82_Email_Architecture_And_AgentMail_Source_Of_Truth_2026-03-06.md` § Gmail (gws) CLI Fallback → new "Gmail Sent-copy labeling (Phase 1)" subsection. Earlier 2026-05-28 (PM) entry: Two follow-ups to PR #533 shipped as PR #536 after end-to-end production verification. **Follow-up 1: env-scrub.** Field test of #533 exposed `/opt/universal_agent/.env` carries three `GOOGLE_WORKSPACE_CLI_*` env vars set to empty strings; the gws CLI treated those empty paths as authoritative and refused to fall back to `~/.config/gws/credentials.enc`, failing with `GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE points to , but file does not exist`. `_send_via_gmail_cli` now builds a child env that drops any `GOOGLE_WORKSPACE_CLI_*` var whose value is blank/whitespace; non-empty values pass through. New regression test `test_429_fallback_strips_empty_gws_env_vars`. **Follow-up 2: inline TOC + rendered HTML in email body.** Operator complaint after the env-scrub-only resend: "the email digest was in markdown but it's not rendered ... there should have been an index at the beginning of the email." Old design: email body = intro + meta-synthesis only; full per-video content + TOC lived in a separate HTML attachment. New design: `_render_email_body_html(body_md, intro_html=..., full_content=full_content)` emits the WHOLE digest inline — intro + clickable "Jump to a video" TOC + meta-synthesis + per-video retellings + tutorial dispatch — with per-video `<h2 id="vN-slug">` anchors matching the TOC's `href="#vN-slug"` so clicks jump within the same email body. New `_inline_email_styles` walks the rendered HTML and injects per-tag `style="..."` for every supported element (h1/h2/h3/p/ul/ol/li/small/blockquote/code/pre/hr/a/table/th/td) since Gmail strips `<style>` blocks from email bodies. New `_build_inline_toc_html` is the email-body twin of `_build_toc_html` (which still targets the attachment). Backward-compat: callers that omit `full_content` keep legacy behavior. Cron caller updated to pass `full_content=full_content` and adjust intro language. Attachment is still produced and attached as a standalone HTML report for archive / print. **Production field verification:** today's WEDNESDAY YouTube digest re-sent via this new rendering through the AgentMail → gws Gmail fallback chain landed in `kevinjdragan@gmail.com` (Gmail msg `19e6fe6e7eb7252a`) as a rendered HTML email with 18 working TOC anchors at the top — Kevin confirmed "the email was delivered and looks great." Gmail clips bodies over ~102KB and shows "View entire message" at the bottom (the WEDNESDAY digest body is 129KB), but the TOC sits above the clip line so all 18 entries remain clickable without expanding. Tests: 6 new in `test_youtube_daily_digest_email_html.py` (TOC heading present, link↔anchor parity, h2/h3/p have inline styles, TOC box self-styled, legacy back-compat). Canonical-doc updates: `docs/03_Operations/82_Email_Architecture_And_AgentMail_Source_Of_Truth_2026-03-06.md` § Gmail (gws) CLI Fallback on AgentMail 429 expanded with env-scrub + field-verification subsections; new § "Daily YouTube Digest — Email Layout" documenting the inline-TOC design, Gmail clip threshold, inline-styling mechanic, and backward-compat. Earlier 2026-05-28 entry: AgentMail residual-volume investigation report (NEW, docs-only). `docs/proactive_signals/agentmail_residual_volume_investigation.md` — decision-support investigation into email-volume sources *outside* the shipped insight-pipeline consolidation. Quantifies the reduction (`vp.agents` 33→1 sent, `oddcity216` 36→6 sent) from production AgentMail + activity-store data. Code-verified root finding: two notification streams diverge on channel default — `_add_notification` (`gateway_server.py:11771`) defaults to `[dashboard,email,telegram]` and is email-eligible; `emit_intelligence_event` (`intelligence_emitter.py:97`) defaults `channels_json='[]'` and is dashboard-only (this is why `proactive_task_failed`, the highest-frequency signal at 64/37h, is never emailed). The only real residual email risk is incident-driven fan-out: `NotificationDispatcher`'s per-`(kind,scope,channel)` 5-min cooldown (`notification_dispatcher.py:40,43-74`) fails to coalesce mass-simultaneous same-kind failures because each event resolves to a distinct scope — the 2026-05-28 00:00–03:53 UTC cascade (5 `execution_missing_lifecycle_mutation` emails DB-confirmed) is the witnessed case. Recommends a per-kind rollup window in the dispatcher as the single highest-impact change. Issue B (`hourly_insight_email` cron) confirmed RESOLVED — absent from prod `cron_jobs.json`, registered `enabled=default="0"` (`gateway_server.py:19208-19225`). Issue C (artifact reminders, `cron_artifact_reminders.py:46-57`) is a bounded 4-step active-window-gated cadence, not a volume problem. Indexed in `docs/README.md` § proactive_signals. Includes two-stream Mermaid flow + code-reference index. No code changes. Earlier 2026-05-28 entry: AgentMail → gws Gmail CLI fallback on HTTP 429 (PR `worktree-agentmail-gmail-fallback`). `src/universal_agent/services/agentmail_service.py:_send_direct` now catches `ApiError(status_code=429)` and shells out to `npx -y @googleworkspace/cli gmail +send` via the new `_send_via_gmail_cli` method. Gated by `UA_AGENTMAIL_GMAIL_FALLBACK=1` (default OFF) so behavior change is conscious. Returns a distinct `status="sent_via_gmail_fallback"` shape with `via="gmail_cli"` so callers/log readers can tell the two paths apart. From: header changes to the gws-authenticated Google account, NOT the AgentMail inbox — replies to fallback-sent messages will not reach Simone's WebSocket listener; acceptable tradeoff for self-addressed digests, documented for outbound to others. Attachments base64-decoded into a tmpdir and passed as repeated `-a` flags (cleaned up in `finally`). HTML body preferred when caller supplied one (`--html` + html arg). Triggered by 2026-05-28 incident: WEDNESDAY YouTube digest fully produced and saved at `/opt/universal_agent/AGENT_RUN_WORKSPACES/daily_digests/2026-05-28_WEDNESDAY_Digest.md` but email delivery failed with "Daily send limit exceeded" (retry-after ~13h). **Operator precondition:** VPS `gws` auth currently reports `token_valid: false` ("Bad Request" on refresh); re-run `npx -y @googleworkspace/cli auth login` on the VPS once before flipping the env flag. Canonical doc update: `docs/03_Operations/82_Email_Architecture_And_AgentMail_Source_Of_Truth_2026-03-06.md` § "Gmail (gws) CLI Fallback on AgentMail 429" — motivation, behavior, From-address tradeoff, HTML, attachment plumbing, operator preconditions, test coverage. Skill doc `.agents/skills/gmail/SKILL.md` updated — note flipped from "do NOT use for Simone's outbound" to "primary use + secondary fallback (transparent, via service-layer code)"; stale `gws gmail send` corrected to `gws gmail +send` (helper command with `+` prefix); `--attach` references updated to `-a/--attach`; HTML send example fixed. Tests: 7 new in `tests/unit/test_agentmail_gmail_fallback.py` — success-path-unchanged, non-429-still-raises, 429-without-flag-reraises, 429-with-flag-triggers-fallback, attachment-plumbing, CLI-failure-propagation, missing-CLI-handling. Earlier 2026-05-27 entry: Residential proxy `PROXY_PROVIDER` default corrected (PR `worktree-proxy-provider-dataimpulse`). Doc 86 (`Residential_Proxy_Architecture_And_Usage_Policy_Source_Of_Truth`) updated to reflect that DataImpulse is the canonical default (Infisical `production` sets `PROXY_PROVIDER=dataimpulse`; code defaults in `youtube_ingest.py`, `tgtg/config.py`, `hooks_service.py` all match). The prior doc claimed Webshare was default, contradicting the runtime. Same PR fixes three hard-coded-Webshare leaks that ignored `PROXY_PROVIDER`: the `.claude/skills/youtube-transcript-metadata` script (now uses provider router), the `.agents/skills/residential-proxy` script (now emits DataImpulse URL by default), and `scripts/check_proxy.py` (default `webshare` → `dataimpulse`). Hooks-service alert fallback string normalized to `DataImpulse` (was inconsistent — `DataImpulse` in one branch, `Webshare` in another). Earlier 2026-05-27 entry: Dashboard "Clear All Completed" bulk-delete fix shipped (PR #498). `web-ui/app/dashboard/todolist/page.tsx:handleDeleteAllCompleted` now issues a single `DELETE /api/v1/dashboard/todolist/completed` instead of looping `DELETE /completed/{task_id}` over the ~80 currently-loaded items. Root cause was pagination: `GET /completed` caps at `limit=80`, so the per-task loop only drained the visible page; on production the DB held 242–317 `status=completed` rows and the next batch bubbled up on reload, looking like the cleared cards were coming back or regenerating. Bulk endpoint at `gateway_server.py:24784` parks every `status=completed` row in one UPDATE. Production verification: 317 completed → 0 in one click (317 rows moved to `parked`, `stale_state=dashboard_hidden`). Canonical doc `docs/02_Subsystems/Task_Hub_Dashboard.md` § 5.2 Write Endpoints gained two new rows documenting the per-task and bulk DELETE endpoints, with an explicit "do not loop the per-task DELETE as a substitute" warning citing PR #498. Earlier 2026-05-27 entry: Task Hub lane semantics corrected (PR `claude/taskhub-in-progress-truth`). The dashboard's "In Progress" column previously promoted every `status='delegated'` row to in_progress, which made the column appear stuck whenever Atlas or a VP had a backlog (delegated insight briefs piled up showing in_progress even though no agent had claimed them). `_task_hub_board_projection` in `gateway_server.py` now requires either `status='in_progress'`, an active `task_hub_assignments` row in `seized`/`running`, or the legacy `metadata.dispatch.active_agent_id` path. Delegated-but-not-yet-picked-up rows live in **Not Assigned** with a subtle teal left border. Every card now carries a `timeline` array (`created → delegated → picked_up → assignment_<state> → parked/blocked`) rendered as a compact strip under the meta row, plus a `queued_behind: int` count for delegated rows showing how many earlier-delegated tasks are ahead of them per VP target. New "Delete All" bulk button on the In Progress column mirrors the existing one on Not Assigned. Canonical doc update in `docs/02_Subsystems/Task_Hub_Dashboard.md` § 6.1 (lane table + new § 6.1.1 Card Timeline Strip). Earlier 2026-05-25 entry: VP `/goal` integration + failure-rescue PRD published as `docs/01_Architecture/12_VP_Goal_Integration_And_Failure_Rescue_PRD.md` (Status: Draft, awaiting operator approval). Documents seven architectural decisions locked in 2026-05-25 grilling session: (1) `/goal` adoption scope limited to `cody_demo_task` + `cody_scaffold_request` + `tutorial_build` + operator-dispatched Cody (skips `proactive_codie` cleanup which is a search not a goal); (2) universal `BRIEF.md` for all autonomous VP work, `ACCEPTANCE.md` + `goal_condition.txt` only for `/goal`-eligible Cody; (3) every VP failure (all modes) notifies Simone with `failure_count` payload; (4) three new Simone rescue tools — `vp_dispatch_mission_retry`, `vp_dispatch_mission_redispatch_fresh`, `escalate_vp_failure_to_operator`; (5) `/goal` condition co-authored in self-briefing (no translation step); (6) required `COMPLETION.md` + self-attestation before `finalize(completed)` on all paths; (7) "Cody" canonical for human-facing references with `vp.coder.primary` agent_id and `codie/<task>` branch convention preserved. Explicitly NOT building: `needs_review` for VP completions, generalized `cody-work-evaluator` for non-demo work, approval gates between VP completion and email-to-Kevin, sampled audit. Simone clarified to four context-dependent postures: observer (VP success) / rescue-evaluator (VP failure) / full executor (direct user chats with skills + sub-agents + VP delegation tools) / router (Task Hub intake). PRD includes two Mermaid sequence diagrams (success path, failure rescue), one flowchart (Simone's four postures), code-verified citations to all touch points (`claude_cli_client.py:797`, `durable/state.py:1201`, `worker_loop.py:464-498`, `agent_router.py:34`, `HEARTBEAT.md:51-52`, `todo_dispatch_service.py:21,404-405,432-444`, `proactive_codie.py:226`), concrete Python code snippets for every proposed modification, 5-step sequencing table (smoke test → failure-rescue wiring → CC-Simone centralization → self-briefing skill + `/goal` integration → HEARTBEAT/PROMPT reconciliation), risk matrix, success criteria with verification steps. Indexed in `docs/README.md` § 1. Earlier 2026-05-25 entry: Hourly insight redesign + scoring log + evening briefing (PR `claude/hourly-insight-redesign`). New modules `services/hourly_insight_email.py`, `services/proactive_scoring_log.py`, `scripts/hourly_insight_email.py`, `scripts/insight_scoring_health.py`. Adds `proactive_artifacts.delivered_at` column + `proactive_brief_scoring_log` table. Track-B ideation prompt now returns `confidence`. Three new crons: `hourly_insight_email` (top of every hour 6-21 Houston), `evening_briefing` (6 PM), `insight_scoring_health` (Sunday 8 AM). Structural lever: `UA_INSIGHT_HOURLY_EMAIL_ENABLED=0` replaces the previous Simone-pause-email workaround. P2 drift fix: added goal-artifacts endpoint to Ops_API.md; added VP backlog sampling section to Heartbeat_Service.md.

**Last updated:** 2026-05-28 (PM) — Model-routing audit + Opus 4.8 migration + vision-endpoint fix. **Doc (NEW):** `docs/06_Deployment_And_Environments/model_routing_audit_2026-05-28.md` — parallel-agent audit confirming default UA inference routes to the ZAI proxy (`ANTHROPIC_BASE_URL=https://api.z.ai/api/anthropic`, GLM-5.1→opus, GLM-5-turbo→sonnet/haiku via `utils/model_resolution.py`), with real-Anthropic reached only by Cody anthropic-mode (CLI scrubs `ANTHROPIC_*` + OAuth + `--model claude-opus-4-8`), Cody demo workspaces (`_scrubbed_env`), Kevin's interactive `claude` launcher, and the dependency/`_smoke` anthropic-native smokes. Includes a full per-site table of every `Anthropic()`/`AsyncAnthropic()` client + `claude` CLI/SDK spawn classified ZAI vs real. **Code fix:** `gateway_server.py` `/api/v1/vision/describe` (backs dashboard image-paste-to-vision, `SimoneChatBar.tsx:125-176`) was hardcoding `https://api.anthropic.com/v1/messages` while sending the daemon's ZAI key + a GLM model string, and called `resolve_opus()` without importing it (`# noqa: F821`, issue #177) → `NameError` on every call. Rewired to route via `ANTHROPIC_BASE_URL` (ZAI, `glm-5.1` vision lane), added the `resolve_opus` import, broadened key resolution to the standard `ANTHROPIC_API_KEY`→`ANTHROPIC_AUTH_TOKEN`→`ZAI_API_KEY` chain. **Model migration (bump-to-newest):** deprecated/dated model strings updated across production + standalone subsystems — Cody CLI default `claude-opus-4-7`→`claude-opus-4-8` (`claude_cli_client.py:320`); `claude-sonnet-4-20250514`→`claude-sonnet-4-6` in `URW/phase0_runner.py`, `URW/phase1_receipt_runner.py`, `RLM/types.py`, `RLM/cli.py`, `security_evals/promptfoo_redteam.yaml`; dated Haiku→`claude-haiku-4-5` in `services/dependency_upgrade.py` + `discord_intelligence/triage.py`; prose/glossary refreshed in `csi_intelligence_pass.py`, `cody_token_tracking.py`, `execution_engine.py`. No breaking-change params at any swapped site. Tests excluded; `security_evals/promptfoo_redteam_generated.yaml` left for regeneration (generated artifact). Indexed in `docs/README.md` § Claude environments. Earlier 2026-05-28 entry: Artifact-reminder cadence capped at 3 emails (day-7 reminder removed). Deferred-backlog item from the AgentMail residual-volume investigation (Issue C). `services/cron_artifact_reminders.py`: removed the `sent_day3 → sent_day7` transition from `_STATE_TRANSITIONS` so `sent_day3` is terminal — cadence is now initial + same-day nudge + day-3 (was + day-7). The "final reminder" copy and `(final)` headline are repurposed onto the day-3 email; `DAY7_DELAY_S` and the `sent_day7` headline removed. Legacy in-flight rows at `sent_day3`/`sent_day7` transition cleanly to `stopped` (no day-7 email). Reminders stay active-window-gated (06:00–22:00 Houston). Tests: `test_cron_artifact_reminders.py` — `test_day3_to_day7_transition`/`test_day7_terminal_stops` replaced by `test_day3_is_terminal` (final-reminder copy + stop) and `test_legacy_sent_day3_row_stops_without_day7`. Investigation note (Issue C "ran on wrong topic" FYI): grep-confirmed NO distinct emitter — it was an ordinary `cron_artifact_notifier` initial email whose artifact title happened to read "ran on wrong topic"; no clean per-message downgrade exists, so intentionally not actioned. Earlier 2026-05-28 entry: Operational-alert email noise: daemon execution-timeout + continuity alerts routed dashboard-only. Follow-up to the AgentMail residual-volume investigation, driven by a live diagnosis of two `gws`-delivered alerts (`daemon_execution_timeout:1800s` and `continuity_alert`). Root causes (code-verified): Alert 1 = a single `daemon_simone_todo` insight-brief turn wedging inside one Claude SDK turn for 30 min (NOT event-loop starvation — the gateway stayed responsive; the watchdog at `gateway_server.py:4378` cancels cleanly and the dispatcher re-runs); it emailed only because `_cancel_session_execution` suppressed the notification for `daemon_idle_timeout` but not `daemon_execution_timeout`. Alert 2 = benign session-not-found 404s (`get_session_info`, `:31675`) incrementing `resume_failures` and tripping the 3-in-900s `continuity_alert` threshold, re-firing because the active-alert set is in-memory and resets on every restart. Both are `severity="warning"` → default-routed to the email channel. Fix: `_add_notification` gains an optional `channels` per-record override (`gateway_server.py:~11784`, applied at the record-creation `channels` field); `_cancel_session_execution` emits `daemon_execution_timeout` cancellations with `channels=["dashboard"]` (dashboard-visible, no email; `daemon_idle_timeout` stays fully suppressed; other cancellations still email); `_sync_continuity_notifications` emits `continuity_alert` with `channels=["dashboard"]`. The underlying wedge (bounding the insight-brief turn) is deferred for operator sign-off (behavior change to daemon scheduling). Tests: 3 new DB-backed in `tests/unit/test_notification_channels_override.py` (dashboard-only override, default-includes-email, channel normalization) + 1 new discriminator guard in `test_session_cancel_notification_filter.py`. No schema change; dashboard still shows every row. Earlier 2026-05-28 (PM) entry: Notification alert rollup shipped (per-kind email rollup window in `NotificationDispatcher`). Highest-impact fix from the AgentMail residual-volume investigation (Issue A): incident-driven alert-email fan-out. `services/notification_dispatcher.py` gains a per-`kind` rollup window layered above the existing per-`(kind,scope)` 5-min cooldown — the first alert of a kind sends immediately and opens a window (`_rollup_start`); same-kind alerts arriving while the window is open are buffered (`_rollup_buffer`, marked delivered so they can't re-surface) instead of emailed individually; `_flush_expired_rollups` (called each `dispatch_pending_once` tick) emits ONE rollup email per kind whose window expired with ≥1 buffered event, or silently clears the window for an isolated alert. Email channel only (v1; Telegram unchanged). New env: `UA_NOTIFICATION_ROLLUP_ENABLED` (default `1`, set `0` to revert to exact legacy behavior) and `UA_NOTIFICATION_ROLLUP_WINDOW_SECONDS` (default `180`), wired at the dispatcher construction site in `gateway_server.py:~15114`. Alerts remain 24/7 (infrastructure-event handlers — no dormancy); rollup changes only batching. New spec `docs/proactive_signals/notification_alert_rollup_spec.md` (buffer/flush flowchart + config table + test plan), indexed in `docs/README.md`. Tests: 12 new in `tests/unit/test_notification_dispatcher_rollup.py` (first-send-opens-window, buffer-then-one-rollup, isolated-no-rollup, independent per-kind windows, rollup-disabled-legacy, cooldown-still-applies, plus `_format_rollup_email` subject/overflow/HTML-escape). Existing `test_notification_dispatcher_pure.py` unchanged & green. No schema change; dashboard still shows every individual row. **Last updated:** 2026-05-28 (PM) — Two follow-ups to PR #533 shipped as PR #536 after end-to-end production verification. **Follow-up 1: env-scrub.** Field test of #533 exposed `/opt/universal_agent/.env` carries three `GOOGLE_WORKSPACE_CLI_*` env vars set to empty strings; the gws CLI treated those empty paths as authoritative and refused to fall back to `~/.config/gws/credentials.enc`, failing with `GOOGLE_WORKSPACE_CLI_CREDENTIALS_FILE points to , but file does not exist`. `_send_via_gmail_cli` now builds a child env that drops any `GOOGLE_WORKSPACE_CLI_*` var whose value is blank/whitespace; non-empty values pass through. New regression test `test_429_fallback_strips_empty_gws_env_vars`. **Follow-up 2: inline TOC + rendered HTML in email body.** Operator complaint after the env-scrub-only resend: "the email digest was in markdown but it's not rendered ... there should have been an index at the beginning of the email." Old design: email body = intro + meta-synthesis only; full per-video content + TOC lived in a separate HTML attachment. New design: `_render_email_body_html(body_md, intro_html=..., full_content=full_content)` emits the WHOLE digest inline — intro + clickable "Jump to a video" TOC + meta-synthesis + per-video retellings + tutorial dispatch — with per-video `<h2 id="vN-slug">` anchors matching the TOC's `href="#vN-slug"` so clicks jump within the same email body. New `_inline_email_styles` walks the rendered HTML and injects per-tag `style="..."` for every supported element (h1/h2/h3/p/ul/ol/li/small/blockquote/code/pre/hr/a/table/th/td) since Gmail strips `<style>` blocks from email bodies. New `_build_inline_toc_html` is the email-body twin of `_build_toc_html` (which still targets the attachment). Backward-compat: callers that omit `full_content` keep legacy behavior. Cron caller updated to pass `full_content=full_content` and adjust intro language. Attachment is still produced and attached as a standalone HTML report for archive / print. **Production field verification:** today's WEDNESDAY YouTube digest re-sent via this new rendering through the AgentMail → gws Gmail fallback chain landed in `kevinjdragan@gmail.com` (Gmail msg `19e6fe6e7eb7252a`) as a rendered HTML email with 18 working TOC anchors at the top — Kevin confirmed "the email was delivered and looks great." Gmail clips bodies over ~102KB and shows "View entire message" at the bottom (the WEDNESDAY digest body is 129KB), but the TOC sits above the clip line so all 18 entries remain clickable without expanding. Tests: 6 new in `test_youtube_daily_digest_email_html.py` (TOC heading present, link↔anchor parity, h2/h3/p have inline styles, TOC box self-styled, legacy back-compat). Canonical-doc updates: `docs/03_Operations/82_Email_Architecture_And_AgentMail_Source_Of_Truth_2026-03-06.md` § Gmail (gws) CLI Fallback on AgentMail 429 expanded with env-scrub + field-verification subsections; new § "Daily YouTube Digest — Email Layout" documenting the inline-TOC design, Gmail clip threshold, inline-styling mechanic, and backward-compat. Earlier 2026-05-28 entry: AgentMail residual-volume investigation report (NEW, docs-only). `docs/proactive_signals/agentmail_residual_volume_investigation.md` — decision-support investigation into email-volume sources *outside* the shipped insight-pipeline consolidation. Quantifies the reduction (`vp.agents` 33→1 sent, `oddcity216` 36→6 sent) from production AgentMail + activity-store data. Code-verified root finding: two notification streams diverge on channel default — `_add_notification` (`gateway_server.py:11771`) defaults to `[dashboard,email,telegram]` and is email-eligible; `emit_intelligence_event` (`intelligence_emitter.py:97`) defaults `channels_json='[]'` and is dashboard-only (this is why `proactive_task_failed`, the highest-frequency signal at 64/37h, is never emailed). The only real residual email risk is incident-driven fan-out: `NotificationDispatcher`'s per-`(kind,scope,channel)` 5-min cooldown (`notification_dispatcher.py:40,43-74`) fails to coalesce mass-simultaneous same-kind failures because each event resolves to a distinct scope — the 2026-05-28 00:00–03:53 UTC cascade (5 `execution_missing_lifecycle_mutation` emails DB-confirmed) is the witnessed case. Recommends a per-kind rollup window in the dispatcher as the single highest-impact change. Issue B (`hourly_insight_email` cron) confirmed RESOLVED — absent from prod `cron_jobs.json`, registered `enabled=default="0"` (`gateway_server.py:19208-19225`). Issue C (artifact reminders, `cron_artifact_reminders.py:46-57`) is a bounded 4-step active-window-gated cadence, not a volume problem. Indexed in `docs/README.md` § proactive_signals. Includes two-stream Mermaid flow + code-reference index. No code changes. Earlier 2026-05-28 entry: AgentMail → gws Gmail CLI fallback on HTTP 429 (PR `worktree-agentmail-gmail-fallback`). `src/universal_agent/services/agentmail_service.py:_send_direct` now catches `ApiError(status_code=429)` and shells out to `npx -y @googleworkspace/cli gmail +send` via the new `_send_via_gmail_cli` method. Gated by `UA_AGENTMAIL_GMAIL_FALLBACK=1` (default OFF) so behavior change is conscious. Returns a distinct `status="sent_via_gmail_fallback"` shape with `via="gmail_cli"` so callers/log readers can tell the two paths apart. From: header changes to the gws-authenticated Google account, NOT the AgentMail inbox — replies to fallback-sent messages will not reach Simone's WebSocket listener; acceptable tradeoff for self-addressed digests, documented for outbound to others. Attachments base64-decoded into a tmpdir and passed as repeated `-a` flags (cleaned up in `finally`). HTML body preferred when caller supplied one (`--html` + html arg). Triggered by 2026-05-28 incident: WEDNESDAY YouTube digest fully produced and saved at `/opt/universal_agent/AGENT_RUN_WORKSPACES/daily_digests/2026-05-28_WEDNESDAY_Digest.md` but email delivery failed with "Daily send limit exceeded" (retry-after ~13h). **Operator precondition:** VPS `gws` auth currently reports `token_valid: false` ("Bad Request" on refresh); re-run `npx -y @googleworkspace/cli auth login` on the VPS once before flipping the env flag. Canonical doc update: `docs/03_Operations/82_Email_Architecture_And_AgentMail_Source_Of_Truth_2026-03-06.md` § "Gmail (gws) CLI Fallback on AgentMail 429" — motivation, behavior, From-address tradeoff, HTML, attachment plumbing, operator preconditions, test coverage. Skill doc `.agents/skills/gmail/SKILL.md` updated — note flipped from "do NOT use for Simone's outbound" to "primary use + secondary fallback (transparent, via service-layer code)"; stale `gws gmail send` corrected to `gws gmail +send` (helper command with `+` prefix); `--attach` references updated to `-a/--attach`; HTML send example fixed. Tests: 7 new in `tests/unit/test_agentmail_gmail_fallback.py` — success-path-unchanged, non-429-still-raises, 429-without-flag-reraises, 429-with-flag-triggers-fallback, attachment-plumbing, CLI-failure-propagation, missing-CLI-handling. Earlier 2026-05-27 entry: Residential proxy `PROXY_PROVIDER` default corrected (PR `worktree-proxy-provider-dataimpulse`). Doc 86 (`Residential_Proxy_Architecture_And_Usage_Policy_Source_Of_Truth`) updated to reflect that DataImpulse is the canonical default (Infisical `production` sets `PROXY_PROVIDER=dataimpulse`; code defaults in `youtube_ingest.py`, `tgtg/config.py`, `hooks_service.py` all match). The prior doc claimed Webshare was default, contradicting the runtime. Same PR fixes three hard-coded-Webshare leaks that ignored `PROXY_PROVIDER`: the `.claude/skills/youtube-transcript-metadata` script (now uses provider router), the `.agents/skills/residential-proxy` script (now emits DataImpulse URL by default), and `scripts/check_proxy.py` (default `webshare` → `dataimpulse`). Hooks-service alert fallback string normalized to `DataImpulse` (was inconsistent — `DataImpulse` in one branch, `Webshare` in another). Earlier 2026-05-27 entry: Dashboard "Clear All Completed" bulk-delete fix shipped (PR #498). `web-ui/app/dashboard/todolist/page.tsx:handleDeleteAllCompleted` now issues a single `DELETE /api/v1/dashboard/todolist/completed` instead of looping `DELETE /completed/{task_id}` over the ~80 currently-loaded items. Root cause was pagination: `GET /completed` caps at `limit=80`, so the per-task loop only drained the visible page; on production the DB held 242–317 `status=completed` rows and the next batch bubbled up on reload, looking like the cleared cards were coming back or regenerating. Bulk endpoint at `gateway_server.py:24784` parks every `status=completed` row in one UPDATE. Production verification: 317 completed → 0 in one click (317 rows moved to `parked`, `stale_state=dashboard_hidden`). Canonical doc `docs/02_Subsystems/Task_Hub_Dashboard.md` § 5.2 Write Endpoints gained two new rows documenting the per-task and bulk DELETE endpoints, with an explicit "do not loop the per-task DELETE as a substitute" warning citing PR #498. Earlier 2026-05-27 entry: Task Hub lane semantics corrected (PR `claude/taskhub-in-progress-truth`). The dashboard's "In Progress" column previously promoted every `status='delegated'` row to in_progress, which made the column appear stuck whenever Atlas or a VP had a backlog (delegated insight briefs piled up showing in_progress even though no agent had claimed them). `_task_hub_board_projection` in `gateway_server.py` now requires either `status='in_progress'`, an active `task_hub_assignments` row in `seized`/`running`, or the legacy `metadata.dispatch.active_agent_id` path. Delegated-but-not-yet-picked-up rows live in **Not Assigned** with a subtle teal left border. Every card now carries a `timeline` array (`created → delegated → picked_up → assignment_<state> → parked/blocked`) rendered as a compact strip under the meta row, plus a `queued_behind: int` count for delegated rows showing how many earlier-delegated tasks are ahead of them per VP target. New "Delete All" bulk button on the In Progress column mirrors the existing one on Not Assigned. Canonical doc update in `docs/02_Subsystems/Task_Hub_Dashboard.md` § 6.1 (lane table + new § 6.1.1 Card Timeline Strip). Earlier 2026-05-25 entry: VP `/goal` integration + failure-rescue PRD published as `docs/01_Architecture/12_VP_Goal_Integration_And_Failure_Rescue_PRD.md` (Status: Draft, awaiting operator approval). Documents seven architectural decisions locked in 2026-05-25 grilling session: (1) `/goal` adoption scope limited to `cody_demo_task` + `cody_scaffold_request` + `tutorial_build` + operator-dispatched Cody (skips `proactive_codie` cleanup which is a search not a goal); (2) universal `BRIEF.md` for all autonomous VP work, `ACCEPTANCE.md` + `goal_condition.txt` only for `/goal`-eligible Cody; (3) every VP failure (all modes) notifies Simone with `failure_count` payload; (4) three new Simone rescue tools — `vp_dispatch_mission_retry`, `vp_dispatch_mission_redispatch_fresh`, `escalate_vp_failure_to_operator`; (5) `/goal` condition co-authored in self-briefing (no translation step); (6) required `COMPLETION.md` + self-attestation before `finalize(completed)` on all paths; (7) "Cody" canonical for human-facing references with `vp.coder.primary` agent_id and `codie/<task>` branch convention preserved. Explicitly NOT building: `needs_review` for VP completions, generalized `cody-work-evaluator` for non-demo work, approval gates between VP completion and email-to-Kevin, sampled audit. Simone clarified to four context-dependent postures: observer (VP success) / rescue-evaluator (VP failure) / full executor (direct user chats with skills + sub-agents + VP delegation tools) / router (Task Hub intake). PRD includes two Mermaid sequence diagrams (success path, failure rescue), one flowchart (Simone's four postures), code-verified citations to all touch points (`claude_cli_client.py:797`, `durable/state.py:1201`, `worker_loop.py:464-498`, `agent_router.py:34`, `HEARTBEAT.md:51-52`, `todo_dispatch_service.py:21,404-405,432-444`, `proactive_codie.py:226`), concrete Python code snippets for every proposed modification, 5-step sequencing table (smoke test → failure-rescue wiring → CC-Simone centralization → self-briefing skill + `/goal` integration → HEARTBEAT/PROMPT reconciliation), risk matrix, success criteria with verification steps. Indexed in `docs/README.md` § 1. Earlier 2026-05-25 entry: Hourly insight redesign + scoring log + evening briefing (PR `claude/hourly-insight-redesign`). New modules `services/hourly_insight_email.py`, `services/proactive_scoring_log.py`, `scripts/hourly_insight_email.py`, `scripts/insight_scoring_health.py`. Adds `proactive_artifacts.delivered_at` column + `proactive_brief_scoring_log` table. Track-B ideation prompt now returns `confidence`. Three new crons: `hourly_insight_email` (top of every hour 6-21 Houston), `evening_briefing` (6 PM), `insight_scoring_health` (Sunday 8 AM). Structural lever: `UA_INSIGHT_HOURLY_EMAIL_ENABLED=0` replaces the previous Simone-pause-email workaround. P2 drift fix: added goal-artifacts endpoint to Ops_API.md; added VP backlog sampling section to Heartbeat_Service.md.

# Documentation Status

**Last updated:** 2026-05-29 — `107_Task_Hub_And_Multi_Channel_Execution_Master_Reference` § 13.0.4 (NEW): terminal status semantics (`completed` vs `parked` vs `cancelled`), the dashboard "clear completed" demotion bug that made Simone's digest miscount archived-done work as deferred backlog (fixed: hidden-completed tasks keep `status=completed` + `stale_state=dashboard_hidden`), and the new `metadata.terminal_disposition` (`completed_with_pr` / `completed_without_pr`) no-op-vs-shipped marker.

**Last updated:** 2026-05-16 (PM) — Two follow-ups to PR #300 (Ghost.build integration). **Doc 1 (NEW):** `docs/02_Subsystems/Ghost_Build_Cody_Demo_Postgres.md` is the canonical subsystem reference for the Ghost integration — replaces the scattered coverage across the provisioning runbook / demo-environments doc / secrets reference with one authoritative document. Includes Mermaid end-to-end sequence diagram (operator → Infisical → daemon → Cody → MCP → ghost.build → demo workspace), file integration map with code-verified citations (every load-bearing file with line anchors), capability matrix listing every tool with use cases, the cleanup contract spec (manifest.json.ghost_databases shape + per-role responsibilities for Cody-during-run, operator-weekly), one-time setup runbook including key-rotation procedure, failure modes catalog, known gaps section listing five named follow-ups (no automated reaper, no per-demo cost telemetry, no data residency, free-tier dependency, no self-host story), and relationship map to other UA subsystems (Demo Execution Environments, ClaudeDevs Intel v2, Task Hub, CSI/vault, Infisical, Memory). Cross-linked from `docs/README.md` § 2 Subsystems. **Doc 2 (NEW, plan only):** `docs/operations/2026-05-16_deploy_ssh_timeout_plan.md` diagnoses the recurring `deploy.yml` exit-255 false-failure (PRs #297, #299, #300 all reported failed but production was live with correct SHA). Root cause: SSH idle eviction during the ~3-minute silent gap inside `install_vps_systemd_units.sh` + service-restart cascade — TCP connection gets pruned by an intermediate idle-killer (Tailscale subnet / NAT). Ranks three remediation options: A) add `-o ServerAliveInterval=30 -o ServerAliveCountMax=120` to the `ssh` command at `deploy.yml:97` (1-line, near-zero risk, **recommended**), B) emit heartbeat lines from inside long-silent scripts (defense in depth, follow-up), C) fire-and-poll deploy refactor (largest, only if A+B insufficient). **Plan only — no code changes shipped.** Awaiting operator approval before implementing Option A. Cross-linked from `docs/README.md` § 4. No production code changed in this PR — pure docs. Earlier 2026-05-16 entry: Ghost.build wired as a Cody-demo-only Postgres capability. New `.mcp.json` in `src/universal_agent/templates/ua_demos_scaffold/` exposes the Ghost MCP server (`npx -y @ghost.build/cli mcp start`) with `${GHOST_API_KEY}` placeholder (per CLAUDE.md MCP rule — never a literal). Demo workspaces — but not top-level UA, and not the `_smoke` workspace — gain on-demand Postgres with pgvector / TimescaleDB / PostGIS / JSONB via tool calls (`ghost_create`, `ghost_sql`, `ghost_fork`, `ghost_delete`, etc.). Operator one-time setup: register `GHOST_API_KEY` in Infisical; the loader injects it into the UA daemon's `os.environ`, `_build_cli_env` propagates to Cody's subprocess, and `${GHOST_API_KEY}` resolves at MCP-server launch. **Demo-only on purpose:** Ghost is hosted-only by Timescale, has no published SLA/durability story, and adds WAN latency — unsuitable for UA's production data plane (Task Hub, CSI, vault, memory). Cleanup contract documented as convention (not code): Cody MUST record created DBs in `manifest.json.ghost_databases` and call `ghost_delete` on success; operator weekly reconciles `ghost list` against `/opt/ua_demos/*/manifest.json` for orphans (free tier 100hr/mo). Canonical doc updates: `docs/operations/demo_workspace_provisioning.md` gained § Step 6 "Ephemeral databases via Ghost" with operator setup + first-run npx cost + cleanup obligation + use/don't-use matrix + two new troubleshooting rows; `docs/06_Deployment_And_Environments/09_Demo_Execution_Environments.md` gained § "Demo-only capabilities (MCP servers in the scaffold)" with demo-only vs top-level distinction; `docs/deployment/secrets_and_environments.md` § "MCP Server Credentials" now lists `GHOST_API_KEY` in a per-key consumption table; `src/universal_agent/templates/ua_demos_scaffold/README.md` § "Ephemeral databases via Ghost" tells Cody the cleanup contract; `.claude/skills/cody-implements-from-brief/SKILL.md` gained § "Ephemeral Postgres via Ghost" with the same contract; `.env.example` MCP block lists the new key. Test coverage: 2 new tests in `tests/unit/test_demo_workspace.py` — `test_provision_ships_ghost_mcp_server` asserts placeholder shape (catches future literal-token leaks) and `test_smoke_workspace_does_not_ship_ghost_mcp` asserts smoke stays minimal. **No production code changes** — pure scaffold + docs + tests. Verification: 30/30 demo workspace tests pass, ruff clean. Earlier 2026-05-14 entry: Two operator-quality-of-life shipments. **PR #273 (MERGED `10c78515`):** Session Baseline Cleanup. New `scripts/claude_session_baseline.py` is invoked by `scripts/_claude_launcher.py` at every `claudereal` session start. When CWD is the UA checkout, it runs a four-case contract: on `main` → `git fetch --prune` + `git pull --ff-only`; on a feature branch whose PR is `MERGED` (or whose remote branch was deleted by auto-merge) → stash runtime gunk (`.omc/state/`, `memory/`, `MEMORY.md`, `temp/`) + `git switch main` + fast-forward + `git branch -D <old>` + drop stash; on an open-PR branch → stay put with status line; on a dirty tree with real source edits → stay put. Best-effort: any failure prints a warning and falls through so `claude` always launches. Closes the local-side gap in the agent PR → auto-merge → deploy → new-session loop (every new terminal used to sit on a now-dead branch after auto-merge deleted the remote). **Also retired `feature/latest2`** in the same PR — local and origin branches deleted, branch protection rule removed. The post-2026-05-10 collapse went one step further: per-task branches off `main` only, no pseudo-trunk of any kind. 16 hermetic tests in `tests/unit/test_claude_session_baseline.py` covering all four cases plus guards (non-UA cwd no-op, missing `gh`, detached HEAD, inner-exception swallowing). **PR #274 (MERGED `561182b5`):** Cron deploy-cancellation classification. Closes the asymmetry where asyncio-coroutine crons cancelled by service shutdown correctly emit `[INFO] Chron Run Cancelled` but subprocess crons (`!script` style like `claude_code_intel_run_report`) SIGTERM'd by the same shutdown were classified as real failures, emitting `[ERROR] Autonomous Task Failed` + `[WARNING] Retrying` email pairs. New `_is_deploy_window_active()` helper in `cron_service.py` returns True iff either (a) the existing `/tmp/ua-deployment-window` flag (already managed by `deploy.yml` for CSI canary suppression) is present, OR (b) the gateway has been up <60 seconds (fallback). When a subprocess exits with a negative return code inside this window: `record.status = "cancelled"` (matches asyncio path exactly), `job.next_run_at = time.time() + 5` persisted, `failure_class="cancelled"`, `retryable=False`. The existing scheduler startup pass at `cron_service.py:604-624` picks up the rescheduled job on next gateway boot via `catch_up_on_restart`. **Reuses every existing primitive — no new tables, startup hooks, or dashboard tiles.** Positive exit codes still surface loudly as `[ERROR]`; negative exit codes outside the deploy window still surface as `[ERROR]` (manual kills, OOM, watchdog). LLM cron path unchanged (in-process, already handled by `asyncio.CancelledError` branch). 13 hermetic tests in `tests/unit/test_cron_deploy_cancellation.py`. **Canonical doc updates:** `docs/06_Deployment_And_Environments/04_Branching_And_Release_Workflow.md` got two new sections — Session Baseline Cleanup (four-case contract table, why-it-exists) and Cron deploy-cancellation classification (under § "Auto-merge for `claude/*` agent PRs"). The same doc's § "Canonical Rule" + § "Environment Mapping" + § "Required Working Method" now reflect the `feature/latest2` retirement; the "Summary" subsection has a new 9-step operating model that includes the session-baseline cleanup as step 1 and step 9. `docs/WORKFLOW.md` § 5 stale recovery tip updated from `git pull --rebase origin feature/latest2` → `git pull --rebase origin main`; banner timestamp bumped. `docs/README.md` § 4A Doc 04 entry expanded to mention the 2026-05-13 Session Baseline Cleanup and 2026-05-14 cron classification. `docs/deployment/ai_coder_instructions.md` substantial update — last-updated banner now 2026-05-14, opening "2026-05-14 simplification" callout added, "Tier 1" branch convention changed from `feature/latest2` to per-task branches off `main`, Agent-Type Matrix row updated, autonomous-mission worktree provisioning changed from `git worktree add /tmp/<bot>_<task_id> feature/latest2` → `git fetch origin main; git worktree add /tmp/<bot>_<task_id> origin/main`, "What NOT to Do" table updated, all stale `feature/latest2` references replaced with current-state guidance. `CLAUDE.md` Git Workflow TL;DR (line 79) rewritten with no `feature/latest2` mention; Rule 8 of Production Verification Rules (line 182) updated to drop the `feature/latest2` example. Plans doc at `plans/2026-05-14_cron_deploy_cancellation_quiet.md` captures the full design rationale + open-questions matrix that drove PR #274 (kept in `plans/` outside `docs/` per convention — design artifacts that informed implementation, not canonical project documentation). **Operator-facing effect:** new terminals always land on fresh `main` (no more "why am I on a stale branch?"). Future deploy-overlapped cron interruptions produce one `[INFO] Chron Run Cancelled` email instead of `[ERROR]` + `[WARNING]` pair; cron re-fires automatically on next gateway boot. No retry storms, no lost work, no scary inbox. **Trigger:** today's PR #273 deploy at `2026-05-14T03:04:22Z` SIGTERM'd the `claude_code_intel_sync` cron mid-run, producing the very scary email pair that PR #274 makes go away — closed-loop fix. Earlier 2026-05-13 entry: CLAUDE.md trim & extraction (operator UX, performance). `CLAUDE.md` reduced 40,373 → ~28,200 chars (-30%) to clear the 40k Claude Code performance warning, while strengthening the documentation-adherence section. **Removed inline:** duplicate Screenshots block (lives in global `~/.claude/CLAUDE.md`); stale 2026-05-06 v2-Phase-2-wiring caveat (tracked in `claudedevs_intel_v2_remaining_work.md`); Codex addendum (moved out — see below); the v2-shakedown 17-PRs/439-tests postmortem narrative and the 2026-05-06 HEARTBEAT.md near-miss narrative (extracted to new doc 130). **Compressed:** Auto-merge PAT war story (PR #232 narrative → one paragraph + link to `ci_cd_pipeline.md`); Operating Hours / Dormancy (full scope rules now linked to `operations/operating_hours_dormancy.md`); Pre-Implementation Reading table cells (verbose prose → file:function pointers); Production Verification Rules 9–12 + Ship-then-Verify cadence A–D (extracted to new doc 130 with one-line callback from CLAUDE.md). **Merged & strengthened:** the parent "Documentation Maintenance Rules" section absorbed into the "Dynamic Documentation Maintenance" subsection — now a single 8-step canonical section with an adherence checklist (read canonical doc first / update during implementation / update both indexes / bump `Last updated` / include diagrams or tables / no unindexed files / when-in-doubt-update). **AGENTS.md symlink broken:** `AGENTS.md` is no longer `→ CLAUDE.md`; it's now a thin file pointing Codex/OpenAI/Antigravity agents at CLAUDE.md for general rules plus the Codex-specific PR-review and browser-debugging deltas. **New canonical docs:** `docs/03_Operations/130_Production_Verification_Rules.md` (postmortem context + Rules A–D Ship-then-Verify cadence + the 8 muscle-memory anti-patterns including `.mcp.json` literal-token, `infisical run` CLI wrapper, auto-resolved `${VAR}` substitution); `docs/03_Operations/131_Implementation_Plan_Quality_Standards.md` (Mermaid diagrams + code-verified citations + summary tables + concrete code snippets + phase-by-phase boundaries — what every implementation plan must include). Both indexed in this file and in `docs/README.md` § 4. **No behavior change.** Earlier 2026-05-12 entry: Interactive Claude launcher hardening (operator UX). `~/.bashrc` aliases `claude` → `scripts/claude_with_mcp_env.sh`; the wrapper now auto-injects `--dangerously-skip-permissions` for interactive sessions and skips that injection for management subcommands (`agents`, `auth`, `doctor`, `mcp`, `plugin`/`plugins`, `project`, `setup-token`, `ultrareview`, `update`/`upgrade`, `install`, `auto-mode`). The new `claude agents` subcommand (v2.1.139+, released 2026-05-11) opens **Agent View** — a session-roster manager for parallel sessions; the wrapper passes it through cleanly because its flag surface is just `--setting-sources` + `-h`. **Canonical doc updates:** `CLAUDE.md` § "Claude Execution Environments" rewritten — three-profile model split into (1) Kevin's interactive coding via aliased `claude`, (2) UA autonomous principals (Simone/Atlas/crons → ZAI), (3) Cody per-task CLI subprocesses (BOTH demo workspaces AND in-environment work → Anthropic Max by default since 2026-05-11 PM per `services/cody_mode.py:34-37`); added "Common past mistakes" callout listing the stale-mental-model errors to avoid (claiming Cody runs on ZAI, claiming Anthropic Max is demos-only, typing `claude agent` singular instead of `claude agents` plural). `docs/06_Deployment_And_Environments/10_Interactive_Coding_Environment.md` § "The three modes" table corrected — "Cody normal work" removed from the Autonomous row (was misleading since 2026-05-11 flip), new Cody row added describing per-task `cody_mode` resolution + Anthropic Max default + Demo workspace settings layer. `docs/development/CLAUDE_CODE_CHEAT_SHEET.md` rewritten — all `bash scripts/claude_with_mcp_env.sh` invocations replaced with bare `claude` (alias-driven), new section 1.1 documenting how the alias works + `claude agents` Agent View walkthrough, common-flags section updated to remove `--dangerously-skip-permissions` line (now auto-injected) and add `claude --bg` + `claude agents` examples. `docs/Glossary.md` ZAI entry corrected — removed "Cody normal work" from the list of ZAI consumers, added a contrast note that Cody now defaults to Anthropic Max with code citation. `docs/WORKFLOW.md` § 1 table expanded to a 5-row layout — Autonomous row no longer lists Cody, new Cody row added with the per-task `cody_mode` field summary, alias mention added to the Interactive row. **Earlier 2026-05-11 PM:** Hermes Ship 4 (Task Hub Observability Protocol) shipped. **Ship 4 (this PR):** LLM cron path observability wired in `cron_service.py` LLM branch (around line 1287) — mirrors the `!script` branch's F-wiring but treats the in-process LLM coroutine as a synthetic subprocess (rc_equiv=0 on success, =1 on timeout/exception; `worker_pid` stays NULL). Try/finally pattern ensures the F-close fires on timeout/exception paths too — an improvement over the `!script` branch's current happy-path-only F-close. **4 housekeeping crons flipped opt-IN** to the protocol: `codie_proactive_cleanup`, `vp_coder_workspace_pruning`, `atlas_direct_dispatch`, `csi_demo_triage_rank`. The "no work-product" rationale from PR #240 was the wrong frame — meta-observability ("is the dispatcher itself running cleanly?") is worth more than the cost of one row per cron source. **New canonical doc** `docs/03_Operations/129_Task_Hub_Observability_Protocol.md` (not 108 — that slot was taken by the Gateway Test Hardening doc) codifies the six-rule protocol: identity, claim ledger, run history, subprocess identity, protocol violation routing, standard recovery verbs. Worked examples for cron jobs / VP missions / webhook handlers; checklist for adding a new task type; helper API reference. `CLAUDE.md` "Pre-Implementation Reading" table gained a row pointing at the new 129 doc as MANDATORY reading before proposing any new async unit of work. **Tests:** 8 new in `test_cron_llm_path_f_observability.py` exercising the LLM-path classifier contract + 4 extending `test_cron_task_hub_linkage.py` to pin that each of the 4 housekeeping crons no longer carries `skip_task_hub_link=True` in its registration source. **Cron source coverage after Ship 4:** 8 work-producing + 4 newly-opted-in = 12 cron sources auto-linked; 2 LLM crons (`autonomous_daily_briefing`, `paper_to_podcast_daily`) now wired via the LLM-branch path — 14/14 cron sources covered. **Future hardening (deferred to Ship 5):** AST-walker CI guard that flags new `subprocess.run / asyncio.create_subprocess_exec / Popen` calls in `src/universal_agent/` not importing from `worker_exit_classifier`. Plan at `docs/reports/hermes-ship-5-lint-guard-plan.md`. **Future hardening (deferred to Ship 6):** ~50 lines into `memory/HEARTBEAT.md` teaching Simone the verb decision tree (when to use `task_re_evaluate` vs `task_request_revision` vs `task_redirect_to`). Plan at `docs/reports/hermes-ship-6-simone-prompt-addendum-plan.md`. Earlier 2026-05-11 PM entry: Hermes Phases D.2 + E + F foundation shipped, plus auto-merge → deploy chain fixed via fine-grained PAT. **Phase D.2 (PR #228, MERGED `9866f18d`):** `re_evaluate` verb now embeds `prior_runs` from `task_hub_runs` alongside `prior_assignments_summary` into the `re_evaluation_context` block — Simone's next claim sees actual per-attempt outcome/summary/error, not just the claim ledger. Failure-context endpoint returns `prior_runs`. Drawer renders an "Attempt history" details block (defaulted open) with color-coded outcomes. 3 new tests in `test_dashboard_failure_context_endpoint.py`. **Phase E (PR #229, MERGED `9c0e0921`):** Cody Anthropic/ZAI toggle on the CLI execution path. New nullable `cody_mode TEXT` column on `task_hub_items` (NULL inherits `UA_CODY_DEFAULT_MODE` env, default "zai"). New `services/cody_mode.py` with `resolve_cody_mode(task)` and `resolve_from_payload(payload)`. `vp_dispatch_mission` plumbs the resolved value into `MissionDispatchRequest.metadata["cody_mode"]` so it lands in `vp_missions.payload_json["metadata"]["cody_mode"]`. `_build_cli_env(cody_mode="anthropic")` strips every `ANTHROPIC_*` env var and forces `CLAUDE_CODE_EXPERIMENTAL_AGENT_TEAMS=1` so the CLI subprocess falls through to workspace-local OAuth (Anthropic Max). `VPAgentHealthTile` appends "· N on Anthropic" to its one-line status and surfaces `evidence.active_anthropic_modes` with per-task details. 13 new tests in `test_cody_mode.py`. **E.2.b SDK in-process adapter routing deferred** to follow-up — in-process SDK calls share parent env so flipping `ANTHROPIC_*` mid-run is race-prone. **Phase F.1 + F.2 foundation (PR #230, MERGED `400da061`):** `worker_pid INTEGER` column on `task_hub_assignments` + `record_worker_pid(...)` helper. `max_runtime_seconds INTEGER` column on `task_hub_items` + `resolve_max_runtime_seconds(task)` helper (env fallback `UA_TASK_DEFAULT_MAX_RUNTIME_SECONDS`, default 7200s). New `services/worker_exit_classifier.py` exporting `WorkerExit`, `WorkerOutcome` Literal, `classify_worker_exit(...)`, and `PROTOCOL_VIOLATION_REASONS` dict for the three sites (cron / vp_cli / demo). Five outcome buckets: `clean_exit_zero`, `clean_exit_zero_no_disposition` (F.3 trigger), `nonzero_exit`, `signaled`, `timeout_killed`. 23 new tests in `test_hermes_phase_f_foundation.py`. Three-site spawn wiring (cron `!script` site, VP CLI client, demo workspace) deferred to per-site follow-up PRs after operator validates the foundation schema. **CI/CD chain fix (PR #232, MERGED `e28b0b79`):** `pr-auto-merge.yml` now uses `secrets.AUTO_MERGE_PAT` (fine-grained PAT scoped to repo with Contents+Pull-requests write) with `secrets.GITHUB_TOKEN` fallback. Previously the GITHUB_TOKEN-driven `gh pr merge --auto` caused GitHub to attribute the resulting squash-merge `push` to GITHUB_TOKEN, which is suppressed from triggering downstream workflows per [GitHub docs](https://docs.github.com/en/actions/security-guides/automatic-token-authentication). Result: Hermes PRs #228, #229, #230 all shipped to main with deploy.yml NEVER firing — production drift until operator ran `gh workflow run deploy.yml --ref main` manually. PAT identity is operator-attributed and is NOT subject to suppression, so deploy.yml now fires automatically. `post-merge-deploy.yml` dispatcher remains as belt-and-suspenders for the bootstrap window when the secret is missing. **Concurrency caveat discovered (open follow-up):** `deploy.yml` lacks a `concurrency:` guard, so when three PRs merged within seconds of each other (#231 Next.js, #226 urllib3, plus #232 chain), three concurrent deploy runs raced on `/opt/universal_agent/.git/index.lock` and all three failed with `fatal: Unable to create '/opt/universal_agent/.git/index.lock': File exists.` One-shot manual recovery: `gh workflow run deploy.yml --ref main` once the stale lock self-clears. Recommended hardening (not yet shipped): add `concurrency: { group: deploy-production, cancel-in-progress: false }` to `deploy.yml`. **Canonical doc updates:** `docs/06_Deployment_And_Environments/04_Branching_And_Release_Workflow.md` § "Auto-merge for `claude/*` agent PRs" expanded with "Why a PAT" sub-section + concurrency caveat sub-section. `docs/deployment/ci_cd_pipeline.md` workflow table updated to mention `AUTO_MERGE_PAT` and concurrency, plus "Why a PAT" and "Concurrency caveat" sub-sections. `docs/reports/hermes-adaptation-phased-plan-2026-05-10.md` Implementation Status table now has rows for D.2, E (CLI path), and F foundation. Earlier 2026-05-11 entry: Hermes Phase C scaffolded + Phase D.1 shipped + canonical-doc maintenance. **Phase C (PR #221, open as of writing):** new `services/atlas_direct_dispatch.py` + `scripts/atlas_direct_dispatch.py` + `_ensure_atlas_direct_dispatch_cron_job()` in `gateway_server.py` — independent cron-registered dispatcher (every 60s, default OFF via `UA_ATLAS_DIRECT_DISPATCH_ENABLED=0`) that bypasses Simone-heartbeat throttle for tasks tagged `metadata.preferred_vp = "vp.general.primary"`. Atomic SQL claim via `json_set` UPDATE prevents double-dispatch. C.2 briefing FYI section in `heartbeat_service.py` surfaces last-15min direct-dispatched missions so Simone retains situational awareness; operator guidance points at `redirect_to(task_id, agent_id='simone')` for take-over. 11 unit tests in `tests/unit/test_atlas_direct_dispatch.py`. v3 JSON-path correctness: filter reads top-level `metadata.preferred_vp` (path set by `proactive_convergence.py:562, 646`), tracking fields live under `metadata.dispatch.atlas_direct_*`. **Phase D.1 (PR #222, MERGED `e00842d2`):** new `task_hub_runs` table parallel to `task_hub_assignments` — assignments is claim-ledger (started/ended/state), runs is outcome record (outcome/summary/metadata/error). `_open_run` + `_close_run` + `list_runs_for_task` helpers in `task_hub.py`. Wired into `claim_next_dispatch_tasks` (post-INSERT) and `finalize_assignments` (post-UPDATE). Additive design: try/except wraps both wire-ins so D.1 bugs never block claim/finalize. 8 unit tests in `tests/unit/test_task_hub_runs.py`. D.2 (re_evaluate prompt addendum + drawer attempt history) is bounded follow-up scoped at ~80-120 LOC. **Canonical doc updates:** `docs/03_Operations/107_Task_Hub_And_Multi_Channel_Execution_Master_Reference_2026-03-31.md` got two new sections — § 13.0.2 (B.2 dashboard failure-context endpoint contract + returned-fields table) and § 13.0.3 (task_hub_runs schema + helpers table + claim/finalize wiring + mermaid sequence diagram of the claim → open run → finalize → close run flow). `docs/03_Operations/cron_job_registration.md` got two new sections — Schedule constraints (`every_seconds ≥ 60s` guard from PR #218, with explicit note that internal async loops like `_vp_stale_reconcile_loop` are exempt) and a new row in the Disable-env table for `atlas_direct_dispatch` (Phase C) plus an explanatory callout. **Operational findings shipped same day (not part of Hermes plan):** PR #218 (`MIN_CRON_INTERVAL_SECONDS=60` guard in `cron_service.add_job`/`update_job`, closes the cron `every_seconds=2` retry-storm vector found in Simone's outbox showing ~30 warning emails over 3 hours from two stale test crons `2df80b6f95` and `4f6f955f91` with literal placeholder commands; both deleted from prod via API). PR #219 (`_vp_stale_reconcile_loop` async task in `gateway_server.py` lifespan that runs the existing on-startup VP stale-mission reconciler every 5 min, default 300s configurable via `UA_VP_STALE_RECONCILE_INTERVAL_SECONDS`, floor 60s, gated by existing `UA_VP_STALE_RECONCILE_ENABLED`; closes the 8h+ stuck-mission gap exposed by the VP Coder mission `vp-mission-aac933ded3b0d7238eabee89` that sat in `status=running` for 8.5h after its external daemon stopped polling and after the work it was assigned shipped via PR #213). PR #220 — Hermes Phase B.2 dashboard failure-context endpoint + drawer UI + 4 unstick buttons + 6 unit tests, MERGED `c1c121e5`. Earlier 2026-05-11 entry: Hermes Phase B.2 shipped: dashboard failure-context endpoint `GET /api/v1/dashboard/todolist/tasks/{task_id}/failure-context` in `gateway_server.py` (returns last_disposition_reason, retry counters, side-effect summary, re_evaluation_context, revision_round, max_retries, prior_assignments). Frontend `web-ui/app/dashboard/todolist/page.tsx` drawer gets a FailureContext block + 4 unstick verb buttons (Rehydrate / Re-evaluate / Redirect to / Request revision) that wrap the B.1 `perform_task_action` verbs. 6 unit tests in `tests/unit/test_dashboard_failure_context_endpoint.py` covering 404, fresh-open zero counters, wedged-task populated payload, re_evaluation_context post-`re_evaluate`, revision_round post-`request_revision`, and prior_assignments shape + newest-first ordering. Phase B.1 status corrected in plan doc (was IN FLIGHT → now SHIPPED via #198). Plus two ops-burden-reduction follow-ups shipped same day: PR #218 added MIN_CRON_INTERVAL_SECONDS=60 guard in `cron_service.add_job`/`update_job` (closes the cron `every_seconds=2` retry-storm vector); PR #219 added `_vp_stale_reconcile_loop` async task in `gateway_server.py` lifespan that runs the existing on-startup VP stale-mission reconciler every 5 min (closes the 8h stuck-mission gap when external VP daemons stop polling). Earlier 2026-05-11 entry: Hermes Continued handoff docs added — `docs/reports/hermes_continued.md` consolidates state of the Hermes adaptation for the next coding agent: shipped/in-flight/remaining phases A-F, B.2 WIP anchor at `claude/hermes-phase-b2-dashboard` @ `0c7df57`, the cron-error mailbox triage requirement that's blocking further B.2 work, the recovery story for PRs #193-#198, post-2026-05-10 branch model context, local-dev affordances, file map, and a first-hour checklist. Companion doc `docs/reports/hermes_continued_start_instructions.md` is the operator-facing launch guide: desktop vs VPS launch options, the 4-check capability self-test (AgentMail / Anthropic Max plan / `just dev` / git state), the model mix (interactive coding = Anthropic, UA autonomous = ZAI, demos = Anthropic, Cody still ZAI until Hermes Phase E lands), troubleshooting. Both docs indexed in `docs/README.md` § 11. **Why:** the cloud-sandbox session that drafted B.1 + B.2 couldn't reach AgentMail MCP (so couldn't triage cron errors) and couldn't run `just dev` (so couldn't visually verify the B.2 drawer). Continuing the work needs a session launched via `scripts/claude_with_mcp_env.sh` from desktop or VPS to get full MCP + local-dev access. Earlier 2026-05-11 entry: Phase D: dev-safe loop_control + Infisical pollution defense. Second `just dev` verification on Kevin's desktop revealed heartbeat + cron service + 5 cron jobs still firing despite Phase C.2 gates because Infisical's `development` env mirrors `UA_ENABLE_HEARTBEAT=1` / `UA_CRON_ENABLED=1` from prod parity. Phase D tightens `loop_control.should_run_loop` dev semantics: in `UA_RUNTIME_STAGE=development`, truthy `UA_<NAME>_ENABLED` is IGNORED as likely Infisical pollution; only new `UA_DEV_<NAME>_FORCE_ON=1` opts a loop in for testing. `feature_flags.heartbeat_enabled()` and `cron_enabled()` updated to the dev/prod split (legacy `UA_ENABLE_HEARTBEAT` also ignored in dev). Added HQ self-heartbeat gate in `gateway_server.py`. Added CronService defensive: skips loading persisted `cron_jobs.json` in dev regardless of cron_enabled() result — belt-and-suspenders against the 53+ persisted prod cron jobs. New `report_dev_overrides()` startup log emits per-loop decisions in dev so operator sees what's running / what Infisical injected / what's been opted in. New doc `13_Infisical_Dev_Env_Hygiene.md` lists the `UA_*_ENABLED` flags to remove from Infisical's `development` env to silence the "ignored in dev" warnings (optional cleanup; not required for correctness now). Tests: `test_loop_control.py` reorganized for new dev semantics (49 tests); `test_feature_flags_defaults.py` updated (legacy enable wins in prod, IGNORED in dev; new dev opt-in tests). **Production safety:** prod semantics unchanged — `should_run_loop` in non-dev runtime stage honors explicit `UA_<NAME>_ENABLED` as before. Earlier 2026-05-11 entry: local dev / prod separation initiative — initial implementation. Adds `src/universal_agent/loop_control.py` — centralized "should this autonomous loop run?" decision plane: in `UA_RUNTIME_STAGE=development` every loop defaults OFF; explicit `UA_<NAME>_ENABLED` always wins. Refactored gates in `heartbeat_service.py` (DEFAULT_HEARTBEAT_AUTONOMOUS_ENABLED), `services/idle_dispatch_loop.py` (IDLE_POLL_ENABLED), `services/dispatch_service.py` (_stale_sweep_enabled), `services/daemon_sessions.py` (daemon_sessions_enabled — falls through to `should_run_loop` when no explicit override), `gateway_server.py` (_vp_event_bridge_enabled, _vp_stale_reconcile_enabled, plus a new cron-registration master gate UA_CRON_REGISTRATION_ENABLED that short-circuits the 15 `_ensure_*_cron_job()` calls when OFF). New `justfile` at repo root with `dev`/`dev-gateway`/`dev-webui`/`bootstrap`/`dev-kill`/`test`/`lint`/`format`/`preship` recipes. 33-test `tests/unit/test_loop_control.py` covers explicit-override-wins, dev-default-off, prod-uses-prod_default, name normalization, unrecognized-value-falls-through, and `explain_loop_decision`. Doc updates: `12_Local_Dev_Environment.md` § Audit pending strikes the now-completed items (justfile, loop-off audit, index entries) and refines § Triggering autonomous loops with the two patterns that work today (Pattern A: flip individual `UA_<NAME>_ENABLED=1` in `.env`; Pattern B: deferred single-iteration CLIs). New companion doc `docs/operations/2026-05-11_local_dev_initiative_briefing.md` recovered from the orphan API push during PR #199 — shareable cross-session briefing for concurrent AI sessions, listing what's in flight and what files to coordinate on. Both docs indexed in `docs/README.md` § 4A. **Production safety:** zero behavior change in prod — `loop_control.should_run_loop` returns the same answer the old per-loop env-var read would have returned when `UA_RUNTIME_STAGE=production` (or unset, defaulting to non-dev). Earlier 2026-05-11 entry: added `docs/06_Deployment_And_Environments/12_Local_Dev_Environment.md` (contract draft) — canonical runbook for desktop-based local development as part of the dev-vs-prod separation initiative. The VPS is being repositioned as production-only (always-on); the desktop becomes the on-demand local dev environment. Same code as prod, HTTP/UI surface alive, autonomous loops off (heartbeat/cron/dispatch sweep/AgentMail polling/X polling all gated), separate dev SQLite DB, secrets via Infisical `development` env (no `.env` sprawl — only Infisical bootstrap creds live in `.env`). Replaces the HQ Dev Lane runbook content in `05_Local_Runtime_Modes.md` (05 remains canonical for the lane DEFINITION; 12 is the runbook). Doc is explicitly marked "Contract draft" with a § Audit pending punch list of 8 gaps to close before the contract is fully real: `justfile` at repo root, audit each autonomous loop for clean OFF flag, single-iteration CLI entry points for each loop, dev DB story (SQLite default), `development` Infisical env completeness, `snapshot_prod_to_dev.py` script, index updates (this entry), `bootstrap_local_hq_dev.sh` freshness. Doc cross-references 04 (branching), 05 (lane definition), 10 (Claude execution profiles), 11 (VPS-as-dev — reframed as fallback, no longer canonical). Indexes: `docs/README.md` § 4A now lists 12. **Scope this PR:** doc-only — no code changes, no impact on production. Audit + implementation lands in a follow-up PR after operator sign-off on the contract. Earlier 2026-05-11 entry: Hermes Phase A.1+A.2+B.1 + agent-PR auto-merge for `claude/*` branches landed in one recovery PR. Adds: per-task `max_retries` override (`task_hub.py`), `release_stale_assignments` wired into `dispatch_sweep` (`dispatch_service.py`), four operator unstick verbs `rehydrate` / `re_evaluate` / `redirect_to` / `request_revision` (Phase B.1, `task_hub.py`), and `.github/workflows/pr-auto-merge.yml` that auto-enables GitHub auto-merge on `claude/*` PRs to `main`. Two new `docs/reports/` files: `hermes-kanban-tenacity-comparison-2026-05-10.md` (the original investigation) and `hermes-adaptation-phased-plan-2026-05-10.md` (six-phase plan A-F with code anchors and acceptance criteria). Canonical doc updates: `docs/03_Operations/107_Task_Hub_And_Multi_Channel_Execution_Master_Reference_2026-03-31.md` § 13.0 + 13.0.1 (complete `VALID_ACTIONS` table + unstick verbs deep-dive); `docs/deployment/ci_cd_pipeline.md` (added `pr-auto-merge.yml` row + Mermaid flowchart of end-to-end PR-to-production); `docs/06_Deployment_And_Environments/04_Branching_And_Release_Workflow.md` (new "Auto-merge for `claude/*` agent PRs" section). Indexes: both new reports added to `docs/README.md` § 11. **Context:** earlier this session shipped PRs #193-#197 against `feature/latest2` before realizing the post-2026-05-10 model targets `main` directly. Those PRs accumulated on `feature/latest2` without deploying. This recovery PR re-applies the valuable work to `main` directly via cherry-pick, drops the wrong-direction `auto-promote-to-prod.yml` (the develop-aware promote workflow that 2026-05-10's simplification made redundant), and corrects `pr-auto-merge.yml` to target `main` instead of `feature/latest2`. Earlier 2026-05-10 entry: `develop` branch retired — branch model collapsed to PR-only-to-`main`. The chain `feature/latest2 → develop → main` was adding failure modes (silent no-op pushes, stale-branch divergence, mid-chain `git fetch` flakes) without delivering integration value; the aspirational "develop = staging" environment never materialized. New flow: any feature branch → PR → `pr-validate.yml` CI → operator merges → `deploy.yml` fires for the new `main` HEAD. Updated `.github/workflows/pr-validate.yml` (drop `develop` from PR target list), `.github/workflows/deploy.yml` (add `paths-ignore` for docs/state-only commits so nightly drift / openclaw release-sync don't trigger production restart), `.github/workflows/nightly-doc-drift-audit.yml` and `.github/workflows/openclaw-release-sync.yml` (retarget `ref: develop` → `ref: main`, retarget auto-merged PRs `--base develop` → `--base main`). Rewrote `.claude/commands/ship.md` and `.agents/workflows/ship.md` for PR-only-to-`main` flow (`gh pr create --base main`, optional CI watch via `gh pr checks --watch`). Deleted `scripts/promote_to_production.sh` (replaced by PR-merge to `main`). Deleted `.agent/workflows/` (legacy scratch dir — no callers). Updated `scripts/vpsctl.sh:4` and `scripts/update_webshare_proxy_credentials.py:289` to drop `develop`-era language. Heavy doc rewrites: `CLAUDE.md` (4 refs), `README.md:24-25` (Staging/Production lines), `docs/deployment/ai_coder_instructions.md` (Agent-Type → Workflow Matrix retargeted to `main`; tier-1/tier-2 distinction kept but both now arrive at the same PR target), `docs/deployment/architecture_overview.md` (branch model + supported deployment rule), `docs/deployment/ci_cd_pipeline.md` (canonical rule + workflows + 7-step pipeline collapsed to 5-step + branch protection guidance), `docs/06_Deployment_And_Environments/04_Branching_And_Release_Workflow.md` (full rewrite, new mermaid gitGraph dropping develop), `docs/WORKFLOW.md:67` (/ship one-liner). New tests: `tests/unit/test_deploy_prod_workflow_guard.py` adds a `paths-ignore` block assertion; new `tests/unit/test_ship_command_pr_only.py` regression-guards the `/ship` rewrite. **Deferred to follow-up:** delete `develop` branch on origin (keep ≥2 weeks for reversibility insurance). Earlier 2026-05-08 entry: ship-workflow hardening rollout test marker — first /ship cycle after PR #173 merged into develop. This commit is the dummy-payload trigger that brings #173's hardened `.claude/commands/ship.md` and `.agents/workflows/ship.md` from `develop` to `main` so the post-deploy /opt/universal_agent has the new pre-flight SHA preview, post-push verify, --ff-only pulls, and tighter no-changes detection. The next /ship after this one will run the hardened workflow and stderr should show "🔭 Pre-flight ref state (origin):" and "✅ origin/main advanced: <sha1> → <sha2>". Earlier 2026-05-08 entry: post-deploy doc cleanup of the interactive-routing fix. Updated the top status banner of `docs/06_Deployment_And_Environments/10_Interactive_Coding_Environment.md` from "EXECUTED AND VERIFIED 2026-05-07" to a banner that explicitly summarizes the 2026-05-08 follow-up (the `claude_with_mcp_env.sh` collision and its two-layer resolution), so future operators reading the doc top-down see the full timeline before the phase-by-phase content. Rewrote the "Related interactive-claude patterns" launcher-comparison table to describe the actual coexistence behaviour (independent `zai`; ANTHROPIC_*-excluding MCP launcher) rather than the now-stale "no conflict" claim that needed a banner correction. Added an "ANTHROPIC_* exclusion" subsection to `docs/deployment/secrets_and_environments.md` § "MCP Server Credentials" — explains the `exclude_prefixes` mechanism, the dual reason both the routing vars and `ANTHROPIC_API_KEY` are in Infisical (UA Python services need them), and that UA services bypass the exclusion by calling `initialize_runtime_secrets` without the param. Outstanding finding (not addressed in this doc pass): `src/universal_agent/services/cody_implementation.py:50-56` `LEAKY_ANTHROPIC_ENV_VARS` carries the same 5-key blind spot that the launcher had — needs a follow-up PR to switch to prefix-based scrubbing so Cody demos don't fail with the same `Invalid API key` symptom on subprocess `claude` invocations. Earlier 2026-05-08 entry: interactive-routing collision fix — full ANTHROPIC_* exclusion. First pass stripped only the 5 ZAI routing vars (BASE_URL/AUTH_TOKEN/three model keys), but live verification on `ua@uaonvps` failed with `Invalid API key · Fix external API key` — Infisical also holds `ANTHROPIC_API_KEY` (used by `refinement_agent`, `gateway_server` vision, `proactive_signals`, etc.) and Claude Code interpreted that key as an "external API key" overriding OAuth. Revised fix covers the entire `ANTHROPIC_*` namespace in two layers: (1) load-time — `initialize_runtime_secrets()` now accepts `exclude_prefixes: tuple[str, ...]` which is passed through to `_inject_environment_values` and `_load_local_dotenv`; the interactive launcher calls with `exclude_prefixes=("ANTHROPIC_",)` so those keys never enter `os.environ`; UA Python services unchanged (no exclude param); (2) defense-in-depth — `_claude_launcher.py` also strips any `ANTHROPIC_*` key on `os.environ` post-bootstrap to catch leaks from non-Infisical sources (bootstrap `.env`, parent shell). Regression guards: 7 tests in `tests/unit/test_claude_launcher_strip.py` covering both layers; pre-existing `test_initialize_runtime_secrets_local_profile_falls_back_to_dotenv` updated for the new `_load_local_dotenv` kwarg. Operator action after deploy: `cd /opt/ua_demos/_smoke && claude /login` to refresh `/home/ua/.claude/.credentials.json` if stale. Doc update: revised the 2026-05-08 correction note in `docs/06_Deployment_And_Environments/10_Interactive_Coding_Environment.md` § "Related interactive-claude patterns" to reflect the broader strip and OAuth-refresh requirement. Earlier 2026-05-08 entry: documentation-pristine pass — audited all docs touching MCP credentials, ZAI routing, .mcp.json, and post-Phase-B `claude` settings.json behavior. Found two stale-but-active conflicts and fixed both with status-update banners (rather than rewriting their bodies, which would lose the historical design context): `docs/03_Operations/124_Archon_Integration_And_Operations_Guide_2026-04-25.md` § 2 was telling operators that Archon's spawned `claude` subprocess inherits ZAI routing from `~/.claude/settings.json`, which Phase B explicitly stripped — now banner-flagged with two correct wrap patterns; `docs/proactive_signals/claudedevs_intel_v2_design.md` § 8.1 had the same stale claim about the "ZAI-mapped (default UA)" lane — now banner-flagged with cross-refs to current canonical docs and the V3 backfill handoff. Plus shipped the V3 backfill restart handoff doc itself: `docs/proactive_signals/csi_v3_backfill_restart_handoff_2026-05-08.md` — pasteable as the opening prompt of a fresh Claude conversation to pick up the V2/V3 Twitter intel backfill work. Earlier 2026-05-08 entries: canonical-doc enrichment for MCP credentials pattern — `docs/deployment/secrets_and_environments.md` gained a new "MCP Server Credentials (`.mcp.json` placeholders)" section covering the canonical pattern (`${VAR}` placeholders + `scripts/claude_with_mcp_env.sh` launcher), the four anti-patterns to never repeat (literal tokens, `.env`-source wrappers, `infisical run` CLI, ignoring auto-resolution diffs), and a five-step checklist for adding a new MCP server. Two new troubleshooting rows for the "Doctor says MCP needs token" symptom and auto-resolution diff. `docs/06_Deployment_And_Environments/10_Interactive_Coding_Environment.md` gained a "Related interactive-claude patterns" section disambiguating the two launchers (`zai()` for ZAI routing vs `claude_with_mcp_env.sh` for MCP creds — different concerns, both compatible). CLAUDE.md "Pre-Implementation Reading" table gained two new rows (general secrets pattern + `.mcp.json` rule), and three new anti-patterns to never repeat (#6 inlining MCP literals, #7 `infisical run` CLI wrap, #8 committing auto-resolved `.mcp.json` diffs). Earlier 2026-05-08 entry: Hostinger token leak remediation runbook — token committed at `.mcp.json:33` from 2026-02-19 to 2026-05-08; migrated to Infisical and sanitized in `bcc5fd4c`; runbook at `docs/operations/2026-05-08_hostinger_token_remediation.md` covers revocation-first remediation and optional `git-filter-repo` history-rewrite procedure. Earlier 2026-05-08 entry: Z.AI peak-time scheduling finding — Greater-China peak hours map to US night under our `America/Chicago` cron timezone, inverting the "run heavy batch overnight" intuition; 9 of 12 system crons currently fire during China peak; new finding doc at `docs/operations/2026-05-08_zai_peak_time_scheduling.md` with full audit, target windows, and remediation plan; cross-refs added to `docs/03_Operations/cron_job_registration.md` (peak-time guidance section) and `docs/01_Architecture/10_Model_Choice_And_Resolution.md` (capacity-vs-throttling note). Earlier 2026-05-07 entry: Codie rogue-branch recovery — production recovered from a docstring-cleanup `vp_mission` that Simone claimed and executed in-place against `/opt/universal_agent`, crashing CSI cron with a mid-flight `SyntaxError`; recovery walked through capture-before-reset → gateway stop → park-not-cancel SQL → `git reset --hard origin/main` → service restart → manual CSI fire; postmortem at `docs/operations/2026-05-07_codie_rogue_branch_recovery.md`, three follow-ups at `docs/operations/2026-05-07_open_followups.md`. Earlier 2026-05-07 entries: Item 7 — added FINAL DISPOSITION VERIFICATION block to `services/todo_dispatch_service.py:TODO_DISPATCH_PROMPT` so Simone always closes a Task Hub lifecycle action before declaring done; this stops the "Execution Missing Lifecycle Mutation" guardrail in `mission_guardrails.py` from firing post-hoc. Item 3 — landed `vp/worktree_utils.py` and `vp/autonomous_mission_executor.py` with full unit coverage; these are the reusable building blocks for the tier-2 autonomous-mission contract documented in `docs/deployment/ai_coder_instructions.md`. Item 1/4/5/6 operator runbook added at `docs/operations/2026-05-07_handoff_followups.md`. Full session reference: see end-of-session handoff in branch `claude/session-handoff-review-WeP30`. Earlier 2026-05-06 entry: Task Hub three-panel repair, mission-envelope/workstream grouping, ToDo dashboard summaries, Mission Control / Chief-of-Staff evidence ingestion.)

## Development

| Doc | Subject |
|-----|---------|
| **`06_Deployment_And_Environments/12_Local_Dev_Environment.md`** | **CANONICAL (2026-05-11).** Desktop-local dev via `just dev`. Autonomous loops off by default. Bootstrap, daily workflow, loop opt-in pattern (`UA_DEV_<NAME>_FORCE_ON=1`), inspection CLI (`dev_tools`), prod snapshot script. |
| **`development/CLAUDE_CODE_CHEAT_SHEET.md`** | **One-page cheat sheet (2026-05-12 refresh).** How to run Claude Code interactively from your desktop. Now uses the `claude` alias (set in `~/.bashrc` → `scripts/claude_with_mcp_env.sh`) with `--dangerously-skip-permissions` auto-injected for sessions. Covers the new `claude agents` Agent View (v2.1.139+, 2026-05-11), `claude --bg` background sessions, ZAI cheap-mode alternative, slash commands, anti-patterns, troubleshooting. **Read before opening a Claude Code terminal in Antigravity.** |
| `06_Deployment_And_Environments/13_Infisical_Dev_Env_Hygiene.md` | Optional Infisical `development` env cleanup checklist. |
| `operations/2026-05-11_local_dev_initiative_briefing.md` | Cross-session briefing for concurrent AI sessions during the dev/prod separation rollout. |
| `development/LOCAL_DEV.md` | ⚠️ **Superseded by Doc 12 (2026-05-11).** Describes a pre-initiative `.local` Infisical env + shell-script flow that's no longer canonical. Kept for historical reference. |

## ⚠️ MANDATORY: Documentation Rules

This file and `docs/README.md` serve as the **authoritative indexes** for all project documentation.

- All documentation MUST be located strictly within the `docs/` directory.
- Always check this index to update existing documents before creating new ones.
- **Rule:** If you create a new documentation file, you MUST log it and link it in this file and in `docs/README.md`.

## Subsystems (02_Subsystems/)

| Doc | Subject |
|-----|---------| 
| Heartbeat_Service.md | **Canonical source of truth** — heartbeat supervision cycle, health findings contract, mediation flow, daemon stuck-run timeout/crash reporting, and the separation between heartbeat runtimes and dedicated ToDo execution |
| Proactive_Pipeline.md | **Canonical source of truth** — end-to-end proactive pipeline: trusted email triage, Task Hub scoring, dedicated ToDo dispatch lifecycle, delivery-mode heuristics, brainstorm refinement, morning report, 24/7 autonomous ideation (Signal Curator + Reflection Engine), shared daily budget, priority lanes, proactive wiki creation, autonomous morning briefing, 3x daily hybrid intelligence reports (Phase 2), system utilization tracking, LLM analysis, outcome tracking with implicit preference feedback (Phase 3), auto-investigation of failures, memory integration, ideation/convergence **relevance gate** (§3.1.1, non-domain category exclusion at CSI ingest; updated 2026-05-30), roadmap, and test coverage |
| Proactive_Intelligence_Work_Product_Pipeline.md | Planned phased implementation for abundant proactive artifact generation, review candidate emails, feedback learning, CODIE proactive PRs, tutorial build automation, convergence detection, and GWS enhancements |
| Proactive_Autonomous_Work_Product_Project.md | Active implementation reference for the autonomous work-product project: proactive Task Hub history read model, durable session-evidence recaps, three-panel audit links, CODIE cleanup cron lane, feedback/continuation design, and verification record |
| Task_Hub_Dashboard.md | **Canonical source of truth** — frontend design system (`kcd-*` palette, glassmorphism), Kanban component architecture, Mission Control Chief-of-Staff readout (extended in Phases 0–8 by Mission_Control_Intelligence_System.md), embedded/full Agent Flow visual process display, lightweight spotlight and visual preference persistence contracts, dispatcher health, forensic task history, workspace action routing, and derived board-lane UX |
| Architecture_Canvas_View.md | **Canonical source of truth (NEW 2026-05-17)** — navigable Figma-canvas-style architecture map shipped as a single self-contained HTML. Hero rough.js flow-spine + Mermaid drill-down side drawer + Claude environments band. Hand-authored boxes carry verified `source:` pointers; build script renders freshness badges. v1 = E1 + E4 + E8/E9; full 9-exhibit phased roadmap inside. |
| Mission_Control_Intelligence_System.md | **Canonical source of truth** — three-tier operator intelligence stack: tier-0 traffic-light tiles (9 tiles, sweeper-polled, threshold-tuned to production cadence), tier-1 LLM-discovered narrative cards with feedback (thumbs/snooze/comment) + action buttons (Generate Prompt / Send to Codie), tier-2 Chief-of-Staff readout with ledger feedback, Knowledge Ledger surface for retired/archived cards. Includes smart event titles + hide-by-default filter on `/dashboard/events`. Phases 0–4, 6, 7, 8 shipped 2026-05-04; Phase 5 auto-remediation deferred. |
| Memory_System.md | Tiered memory, auto-flush, and LLM Wiki internal-vault sync integration |
| Lossless_Memory.md | Opt-in DAG-based context compression and SQLite history store |
| Durable_Execution.md | Resilience features |
| URW_Orchestration.md | Multi-phase tasks |
| LLM_Wiki_System.md | **Canonical source of truth** — shared wiki engine, external knowledge vault, derived internal memory vault, runtime surfaces, provenance, and integrity/query workflows |
| Discord_Intelligence_System.md | **Canonical source of truth** — architecture for the Discord intelligence and C&C operations, dual-token architecture, 4-stage message processing pipeline (ingestion → signal detection → LLM relevance filter → triage), Phase 5 store-but-hide relevance filter with cross-channel batching and concurrent Haiku-class workers, dashboard signal toggle, configuration reference, and operational runbook |
| ClaudeDevs_X_Intelligence_System.md | **Canonical subsystem reference** — official X API ClaudeDevs lane, packet generation, replay/backfill, linked-source expansion, external Claude Code vault population, candidate ledgering, delivery verification, historical cron cleanup, and the operator skill/report surface. **v2 (2026-05) extends with:** lifted truncation caps (PR 1), 28-day brief + decoupled rebuild (PR 4), full-corpus capability library (PR 5), wiki Memex primitives CREATE/EXTEND/REVISE (PR 2), research grounding subagent (PR 3), multi-lane config (PR 11), Phase 0 dependency-currency observation (PR 6a), demo workspace scaffolding (PR 7). See [v2 design doc](proactive_signals/claudedevs_intel_v2_design.md), [demo workspace runbook](operations/demo_workspace_provisioning.md), [demo execution environments](06_Deployment_And_Environments/09_Demo_Execution_Environments.md). **2026-05-24:** Intel-brief surfacing contract added — Tier 2 and Tier 4 vault writes now produce a `[Intel]` email to Kevin (CC Simone) plus a `proactive_artifacts` row with `artifact_type='intel_brief'` (canonical constant `services.proactive_artifacts.ARTIFACT_TYPE_INTEL_BRIEF`). Triggered by CREATE or material EXTEND of a vault entity. Closes the gap where Tier 4 strategic_follow_up items (e.g. Workload Identity Federation) wrote durable vault knowledge but never reached the operator. See "Intel-Brief Surfacing (Tier 2 & Tier 4)" section in the canonical doc and "Intel-brief surfacing on vault writes" in `memory/HEARTBEAT.md`. **Top-of-doc rewrite (2026-05-05):** now opens with the v2 production architecture (Mermaid diagram, code-to-architecture map, critical invariants), an operational "what to expect" guide (emails, disk paths, Mission Control), a 48-hour audit checkpoint with concrete verification steps, three escape hatches, and a commands quick-reference. Legacy v1 capability list preserved below. |

## Architecture (01_Architecture/)

Written from source code review — these describe the system as it actually exists.

| Doc | Subject |
|-----|---------|
| 01 | System Architecture Overview — component map, services, data stores, deployment topology |
| 02 | Gateway, Sessions & Execution — session model, auth surfaces, execution engine, background services |
| 03 | VP Workers & Delegation — VP lanes (CODIE & ATLAS), mission lifecycle, cross-machine delegation, factory heartbeat, shared VP email identity |
| 04 | Dual Factory and Capability Expansion Brainstorm |
| 05 | Simone-First Orchestration — batch triage, VP delegation lifecycle, two-layer email response, `/btw` sidebar |
| 06 | Comparison: Background Workers vs Simone Orchestration — Deep-dive evaluation of pull-based agent workers vs the event-driven Simone-First orchestration model |
| 07 | Database Architecture — absolute source of truth for database paradigms, schema structure, segregation boundaries, and lifecycle pruning logic |
| 10 | Model Choice and Resolution — Anthropic proxy emulation map, inference fallbacks, and the Capacity Governor loud-failure hook |
| 11 | Archon Comparison & Integration Strategy — critical architectural review comparing Archon to UA, including emulation recommendations for Git Worktree Isolation and integration paths for YAML-driven coding DAGs |
| vp_mission_priority_tiers | VP Mission Priority Tiers (2026-05-27) — semantic `operator_daily` / `operator_signal` / `maintenance` / `background` ranking column above numeric priority. Default tier is `background` (safe — forgotten work runs last). Closes the priority-100-default starvation trap that hid the 2026-05-27 morning briefing miss. Includes the freshness SLA invariant + informational backlog tracking surfaced via Simone's heartbeat context. |

## Root Architecture Docs

| Doc | Subject |
|-----|---------|
| 000 | Pipeline Masterpiece — holistic end-to-end view of input ingress, task hub state machine, Simone orchestration, and the autonomous reflection engines |
| 001 | Agent Architecture (agent_core.py vs main.py) — entry points, AgentSetup sync, URW session reuse |
| 002 | SDK Permissions, Hooks & Subagents — permission model, hooks architecture, subagent patterns |
| 003 | Regression Control and Golden Runs — testing stability, golden run preservation |

## Canonical Source-of-Truth Documents (03_Operations/)

These are the authoritative references for each subsystem. When any other document conflicts, **the canonical doc wins**.

| # | Subject |
|---|---------|
| 07 | WebSocket Architecture (`02_Flows/`) |
| 08 | Auth & Session Security (`02_Flows/`) |
| 82 | Email / AgentMail — includes outbound subject-tag system (`[ACTION/KIND]` + body banner, 2026-05-19), pre-triage deterministic security screening (injection scanner, @agentmail.to auto-quarantine, sender reputation auto-escalation), multi-inbox VP routing (Cody/Atlas direct engagement), CC protocol, FYI suppression, trusted non-action reply auto-completion, stale failed queue auto-cancellation |
| 83 | Webhooks |
| 85 | Infisical Secrets |
| 86 | Residential Proxy — dual-provider architecture (Webshare + DataImpulse), `PROXY_PROVIDER` selection, approved paths, YouTube guardrails |
| 87 | Tailscale |
| 88 | Factory Delegation, Heartbeat & Registry |
| 89 | Runtime Bootstrap, Deployment Profiles & Factory Role |
| 90 | Artifacts, Workspaces & Remote Sync |
| 91 | Telegram |
| 96 | NotebookLM Integration & Research Pipeline — now includes lesson #11 (audio download CDN auth scoping, CLI fallback pattern) |
| 97 | Infisical CLI Reference & Lessons Learned |
| 98 | Agent Skills Directory (`.agents/skills/`) |
| 99_Tutorial | Tutorial Pipeline Architecture & Operations — canonical YouTube tutorial pipeline reference, including **delivery-reminder signals (2026-05-19)** that fire a Telegram ping + dashboard tile on successful AgentMail send, both self-expiring after `UA_DIGEST_REMINDER_TTL_MINUTES` (default 90 min) via `activity_events.expires_at` + `digest_telegram_reminders` sweep, with the hard constraint that the bot only ever deletes its own Telegram messages (never anything Gmail/AgentMail).  Plus the **Daily Digest map-reduce pipeline (2026-05-18)** with per-video retell on `glm-4.5-air` @ conc=3 and meta-synthesis on `glm-5.1`, 50% retell length target, deterministic demo-worthiness gate (score ≥ 70, tier ∉ {low,unknown}, evidence ≠ metadata_only) before tutorial dispatch, per-call retry/FUP-1313 telemetry on `MapResult`, `youtube_digest_compare.py` A/B harness, `required_secrets` preflight including `YOUTUBE_OAUTH_*`, ranked code-prospect dispatch + repopulate pockets, zero-quota SQLite database tracking model, playlist watcher flow, notification kinds, permanent-failure self-clear behavior, dedup, proxy config, env vars, and API endpoints.  Plus the **OAuth token watchdog + one-tap email re-auth (2026-05-29, § 1.1C)**: daily `youtube_oauth_watchdog` cron tests refresh-token liveness + age and emails a signed re-auth button (`/api/v1/youtube-oauth/start`→Google→`/callback` writes token+`MINTED_AT` to prod Infisical); plus the digest subject rename to `Daily YouTube Summaries — <date>`.  Plus the **ranking saturation fixes (2026-05-30, § 1.1B)**: within-band additive scoring in `RETELL_PROMPT` (the map step scores each video in isolation, so coarse bands collapsed onto round anchors — 95, then 85), dynamic `top_n` tie-extension in `_select_tutorial_dispatch_candidates` (`UA_YOUTUBE_DIGEST_AUTO_TUTORIAL_MAX_N`, default `2×top_n`), and the `AgentMailService.shutdown()` httpx-client `aclose()` fix that kills the "Event loop is closed" finalizer traceback across all 6 one-shot `asyncio.run` crons |
| 99 | Documentation Drift Maintenance Pipeline — two-stage nightly audit (heuristic drift detection → VP remediation dispatch), PR persistence, issue batching, verify-before-fix rules |
| 100 | OpenClaw Release Sync Pipeline |
| 101 | VP Agent Identity & Prompt Architecture — CODIE/ATLAS souls, VP system prompt, mission briefing injection
| 102 | E2BIG Kernel Limits and Prompt Architecture — 128KB execution limit constraints and prompt optimization strategy
| 104 | Run/Attempt Lifecycle and Nomenclature Migration Plan — canonical migration plan for run/attempt terminology, packet rollout, rollback scope, and CSI boundary handling |
| 105 | YouTube Tutorial Pipeline Run/Attempt Triage — notification-center audit of the tutorial hook flow after the refactor, plus the closure update that migrated tutorial hooks and cron admission onto durable run/attempt lifecycle handling |
| 107 | Task Hub and Multi-Channel Execution Master Reference — central code-verified map of ingress channels, To Do execution, run workspaces, specialist delegation, delivery, lifecycle events, and Session Explorer file resolution |
| 103 | Debugging Lessons Living Document — reusable lessons from complex incidents: target-host verification, browser-profile state crashes, SHA-first release verification, env-mutation, Gemini API key tiering, NLM-first KB artifact mandate, and the 2026-05-30 "no running event loop" cron lesson (loop-affine helpers detonated by asyncio.to_thread) |
| 109 | LLM Wiki Implementation Status — living tracker for subsystem phases, Phase 3 nightly proactive integration with NLM-backed knowledge bases |
| 110 | LLM Wiki Smoke Test — first manual smoke-test results, runtime issues, and next hardening targets |
| 108 | Gateway Test Hardening And Regression Follow-Up — operational handoff for the April gateway regression pass, including what was fixed, what remains, and how to continue pruning stale tests while development resumes |
| 111 | Discord Operations And Usage Guide — **Canonical source of truth** for operational usage, slash commands, and proactive task generation workflows via the Discord CC Bot |
| 113 | Task Hub Dashboard Read Path Performance — root cause and invariant for keeping dashboard GET endpoints from rebuilding Task Hub or proactive signal state during navigation |
| 114 | CODIE Proactive Pipeline Audit Report — detailed report assessing a manual CODIE proactive cleanup pipeline execution, brittle routing refactoring (PR #103), and system health observations |
| 115 | Proactive Automation Current State Audit — code-verified current-state report for proactive generation, signal curation, reflection, cron registration, tutorial builds, artifact surfacing, and impediments preventing 24/7 proactive throughput |
| 116 | CSI Convergence Proactivity Repair Handoff — shareable implementation context for the CSI convergence repair pass, including fixed blockers, runtime contract, verification evidence, and live-validation next steps |
| 117 | ClaudeDevs X Intelligence Lane Design Handoff — historical design note for a dedicated Claude Code intelligence lane monitoring `@ClaudeDevs`, including source-access findings, tiering, KB/wiki expectations, mirror fallback, and resume checklist |
| 118 | X API And Claude Code Intel Source Of Truth — canonical implementation reference for X API development, `@ClaudeDevs` polling, Infisical keys, packet outputs, operator report artifact generation, skill invocation shape, and cron registration |
| 119 | Task Forge: Autonomous Skill Generation Pipeline — **canonical source of truth** for the meta-skill that converts human intent into structured, reusable task-skills; 7-phase pipeline (intent → scaffold → execute → quality gate → archive/promote), skill maturity model (v0→v3), dispatch integration, hook hardening, skill-creator cooperative relationship, recursive learning loop philosophy, paper-to-podcast case study (cross-skill orchestration, sub-agent delegation anti-pattern, CLI audio fallback) |
| 127 | Project Scaffolder Skill Guide — Canonical operations guide for the `project-scaffolder` skill, including core host dependency and Infisical CLI preflight, structured scaffold metadata, generated project preflight, real-path infrastructure rendering, main-branch initial commits, frontend systemd service generation, canonical `.agents/skills` links, dependency install, GitHub publishing, and remaining operator steps. |
| 120 | ClaudeDevs X Intel VPS Runtime Audit — read-only production audit of the first 19-post `@ClaudeDevs` cron packet, downstream Simone work products, Task Hub/artifact state, email verification, tier-count discrepancy, and recommendations for the Claude Code intelligence wiki pipeline |
| 121 | Test Strategy and Regression Prevention — **canonical source of truth** for test suite layout (1211 unit tests), mandatory pre-ship commands, ContextVar isolation architecture, common failure patterns, CI/CD test gap analysis, maintenance checklist, and the April 20 stabilization history |
| 122 | ClaudeDevs X Intel Implementation Plan — code-verified phased implementation plan for replay/backfill, external vault creation, linked-source expansion, packet candidate ledgering, Task Hub completion evidence mapping, cron/heartbeat workspace separation, operator skill/report surface, and LLM-assisted tiering |
| 123 | Gemini TTS Source of Truth — **canonical source of truth** for Gemini Text-to-Speech: API architecture (Cloud TTS vs Vertex AI vs AI Studio), model registry, auth patterns (service account + ADC), production code samples, voice inventory, markup tags, chunking strategy, region requirements, and lessons learned |
| 124 | Archon Integration and Operations Guide — canonical operations guide for the Archon AI workflow engine integration, covering repository isolation, Infisical wrapping, Claude Code Z.ai endpoint emulation, and VPS deployment architecture |
| 125 | Heartbeat-Safe Proactive Pipeline Operations — canonical handoff and architecture record for isolation of proactive curation pipelines from the system heartbeat (`skip_heartbeat` metadata, `_curation_dispatched` overrides) and ship/rebase operator rules |
| 126 | Candidate Ledger Reconciliation Operations — canonical documentation for the Phase 3 completion of the Packet Candidate Ledger, detailing the `reconcile_packet_candidate_ledger` workflow and TDD maintenance protocols |
| 128 | Artifact Saving and Proactive Work Products — canonical reference for proactive agent artifact generation, centralizing VP code demos to `/home/ua/vpsrepos/`, and mitigating loss inside transient daemon archives |
| 129 | Task Hub Observability Protocol — canonical six-rule standard (identity / claim ledger / run history / subprocess identity / protocol violation routing / standard recovery verbs) for every new async unit of work; worked examples for cron jobs / VP missions / webhook handlers; checklist for adding a new task type; helper API reference |
| 130 | Production Verification Rules — Extended Reference — postmortem context (v2 ClaudeDevs shakedown + 2026-05-06 HEARTBEAT.md near-miss), Ship-then-Verify cadence (Rules A–D: `/api/v1/version` SHA check, backend-vs-UI verification paths, deploy-restart guarantee), and 8 muscle-memory anti-patterns. Long-form companion to `CLAUDE.md` § Production Verification |
| 131 | Implementation Plan Quality Standards — what every implementation plan must include: Mermaid sequence + flowchart diagrams, code-verified citations, summary tables, concrete code snippets (not pseudocode), phase-by-phase boundaries between config/code/prompt changes |

## CSI Subsystem (04_CSI/)

| Doc | Subject |
|-----|----|
| CSI_Master_Architecture.md | **Canonical living doc** — domain taxonomy, SQLite source management, quality scoring, adapter architecture, batch processing, delivery, passive UA ingest boundary for RSS/channel analytics, and subsystem boundaries. Supersedes scattered CSI docs for architecture reference |
| CSI_Convergence_Intelligence_Pipeline.md | **Canonical living doc** — dual-track detection architecture (concrete vs abstract tracks), generating convergence-brief and insight-brief tasks, ingestion hooks, domain taxonomy, and the hourly insight-delivery pipeline (PR `claude/hourly-insight-redesign`, 2026-05-25) |

## Deployment Canonical Docs (deployment/)

| Doc | Subject |
|-----|---------|
| secrets_and_environments.md | **Canonical entry-point** — Infisical secrets, environments, deploy workflow secrets contract |
| architecture_overview.md | Git branching, environmental mapping, service topology, gateway resource limits (MemoryMax/MemoryHigh/TasksMax), and SHA-based release verification |
| ci_cd_pipeline.md | Workflow details, timing, pipeline structure, `/ship` branch guard, post-deploy sync, SHA-first post-release verification, crashloop fail-fast health gate, and deploy-failure email notification |
| infisical_factories.md | Stage naming and machine bootstrap (superseded by secrets_and_environments.md) |
| ai_coder_instructions.md | Coordination rules for external AI coding agents — branch discipline (`feature/latest2` only), commit conventions, and `/ship` handoff protocol |

## Review & Decision Documents

| # | Subject |
|---|---------|
| 93 | Prioritized Cleanup Plan from Canonical Review |
| 94 | Architectural Integration Review |
| 95 | Heartbeat Issue Mediation and Auto-Triage — Simone memory-guided autonomous remediation decisions, Task Hub remediation ingress, and operator escalation boundaries |
| 112 | Codebase Recommendations and Cleanup Agenda |

## Deployment and Environment Continuity Docs (06_Deployment_And_Environments/)

| Doc | Subject |
|-----|---------|
| 04 | Branching and release workflow, including return-to-feature-branch operator rule, `/ship` branch guard enforcement, and SHA-based release confirmation |
| 05 | Local HQ dev vs desktop worker runtime modes |
| 06 | Production deploy incident |
| 07 | Stage-based Infisical and machine bootstrap migration plan |
| 08 | VPS deployment profile stuck at `local_workstation` — final incident record covering the wrong-host false lead and the gateway env-sanitization fix |
| 09 | **Demo Execution Environments** — canonical reference for the dual-environment architecture (UA repo with ZAI-mapped settings vs `/opt/ua_demos/` with Anthropic-native vanilla settings + Max plan OAuth). Explains where each lives on the VPS, how Claude Code picks its environment, common pitfalls and failure modes, dependency-currency smoke matrix across both profiles, and the generalization path for future lanes (Codex, Gemini). |

## Run Reviews (03_Run_Reviews/)

| # | Subject |
|---|---------|
| 01 | Russia Ukraine Report Session (2026-02-06) |
| 02 | Cron Breakthrough Briefing (2026-02-08) |
| 03 | Scheduling Runtime V2 Short Soak Readiness (2026-02-08) |
| 04 | Scheduling Runtime V2 24h Soak In Progress (2026-02-08) |
| 05 | Self-Improving Agent Notes (2026-02-13) |
| 06 | Final Integration Sprint Review (2026-02-14) |
| 07 | Happy Path Review and Markdown Remediation (2026-02-19) |
| 08 | Gemini Interactions Playground Post-Mortem (2026-04-26) — CODIE VP silent failure, constraint key mismatch, ephemeral workspace output; 9 classified errors, 7 prioritized recommendations |
| 09 | Gemini Interactions Demo Run Analysis (2026-04-26) — second-attempt constraint key mismatch (`working_directory` not recognized), glm-5.1 model verified, CODIE completed in ephemeral workspace; 6 prioritized fixes, cross-attempt comparison |



## SDK Phases and Integration Docs

| Doc | Subject |
|-----|---------|
| 004 | Threads Infisical Sync Workflow |
| 008 | Threads Rollout Next Phases |
| 009 | Threads Phase 2 Permission Readiness Runbook |
| 010 | Threads Phase 2/3 Closeout Go/No-Go Report |
| 011 | SDK Phase 5 Accelerated Canary Rollout |
| 012 | NotebookLM Integration |
| 013 | Link Payments Integration — Stripe Link wallet, guardrails, signed-URL card page, agent_purchaser sub-agent, MPP, live-mode flip |

## Reference Docs

| Doc | Subject |
|-----|---------|
| ZAI_OPENAI_COMPATIBLE_SETUP.md | ZAI API setup guide |
| durability_test_matrix.md | Durability testing matrix |
| notebooklm_vps_infisical_runbook.md | NotebookLM VPS setup |
| Glossary.md | Project terminology |

## Reports (reports/)

| Doc | Subject |
|-----|---------|
| todolist-task-pipeline-audit-2026-03-11.md | Historical audit of the early `/dashboard/todolist` pipeline before the dedicated ToDo dispatcher refactor |
| task-hub-happy-path-audit-2026-05-06.md | Code-verified report on why active ToDo executions can fall out of `IN PROGRESS`, how live run workspace lineage is lost, and why viewer hydration falls back to terse `run.log` rows instead of structured `trace.json` detail |
| meaningful-work-taskhub-three-panel-design-2026-05-06.md | Code-verified design proposal for using Task Hub as the ledger for meaningful autonomous work and using sequential parent/child tasks so each phase gets its own run, workspace, and three-panel audit trail |

## Operations (operations/)

| Doc | Subject |
|-----|---------|
| demo_workspace_provisioning.md | **Operator runbook** — one-time setup of `/opt/ua_demos/` on the VPS for Phase 3 demo execution: directory creation, smoke workspace provisioning, Max plan `claude /login` (must be run from inside the demo workspace, not from a ZAI-mapped dir), smoke test verification, troubleshooting table for endpoint mismatch / auth failure / pollution markers |
| 2026-05-07_handoff_followups.md | **Operator runbook** — checklist for the four side-effect items left over from the 2026-05-07 session handoff: CSI v2 Phase 2/3 smoke verification (read `/tmp/csi_smoke_result.log`, decide a/b/c outcome, gate Item 6 backfill), VPS `gh` CLI cleanup (move shadow binary at `/home/ua/.local/bin/gh`), GitHub branch protection (apply Agent-Type → Workflow Matrix from `ai_coder_instructions.md` to main/develop/feature/latest2), and the v2 historical backfill sequence (dry-run → replay → diff → swap → revert). Items in code (Item 7 prompt fix, Item 3 worktree contract) are addressed in branch `claude/session-handoff-review-WeP30` |
| 2026-05-07_codie_rogue_branch_recovery.md | **Incident postmortem** — production recovery on 2026-05-07 where a docstring-cleanup `vp_mission` was claimed by Simone's heartbeat daemon and executed in-place against `/opt/universal_agent`, crashing CSI cron at 08:00 CDT with a mid-flight `SyntaxError` in `durable/state.py:254`. Documents the timeline, what broke (Simone-as-code-author + branch deployed without PR + mid-flight syntax error), four diagnostic dead ends (handoff doc claimed never-fired, wrong DB path, wrong PID flagged, `cancel` SQL resurrected by reconciler), the recovery sequence (capture-before-reset → gateway stop → park SQL → `git reset --hard origin/main` → service restart → manual CSI fire via Ops API), why each gate was needed, and the production state at end of session |
| 2026-05-07_open_followups.md | **Follow-up index** for the 2026-05-07 incident postmortem. Three durable items: (1) reconciler ignores `TERMINAL_STATUSES` and resurrects `cancelled` rows back to `in_progress` (P2, workaround uses `parked`), (2) `deploy.yml` advances `HEAD` and working tree but never advances the local `main` branch pointer, leaving the prod checkout's local `main` 97 commits behind `origin/main` (P2, cosmetic-only blast radius), (3) Simone claimed and executed a code-author `vp_mission` that should have been routed to `vp.coder.primary` or the autonomous-mission worktree contract (P1, recurrence risk; needs investigation of dispatcher capability gating, `HEARTBEAT.md` claim rules, and `vp_mission.preferred_vp` defaults) |
| 2026-05-08_zai_peak_time_scheduling.md | **Active architectural finding** — Z.AI's proxy (`api.z.ai/api/anthropic`) is capacity-limited during Greater-China peak hours (Beijing UTC+8 daytime). Under our default `America/Chicago` cron timezone, **US night maps to China business-day peak** — the inverse of the "run heavy batch overnight" intuition. Audit shows 9 of 12 system crons currently fire during China peak. Recommends shifting heavy LLM-bound proactive jobs to US 12:00–17:00 CDT (= China deep-night off-peak), adds a peak-time guidance section to `docs/03_Operations/cron_job_registration.md`, and a peak-time note to `docs/01_Architecture/10_Model_Choice_And_Resolution.md`. Also frames the operator-idle-detection follow-up (gate heavy lanes on dashboard activity stamp / Antigravity session telemetry / calendar busy) and the role of the `csi_llm_pacing` token-bucket module as a backstop rather than the primary fix. **Read before picking `default_cron` for any new LLM-bound proactive cron.** |
| 2026-05-08_hostinger_token_remediation.md | **Operator runbook** for the Hostinger API token leak. Token was committed as a literal at `.mcp.json:33` from 2026-02-19 through 2026-05-08 (~78 days, 119 affected refs); migrated to Infisical (`production / HOSTINGER_API_TOKEN`) and sanitized to `${HOSTINGER_API_TOKEN}` placeholder in commit `bcc5fd4c`. Doc covers the threat model (anyone who fetched the repo during the window has the literal in their local .git; GitHub caches dangling commits ~90 days post-rewrite; forks/PR refs/CI snapshots all retain the value — so history rewrite cannot fully scrub the leak), the **revocation-first remediation** (Hostinger UI → curl-verify 401/403 — once the leaked literal returns 401, it's a dead string and the leak is neutralized), and the optional `git-filter-repo` history-rewrite procedure with all the operational gotchas (force-push, collaborator re-clone, GitHub 90-day dangling-commit cache, deleted-PR refs, fork retention). **Recommendation: revoke and stop; history rewrite isn't worth the operational cost for this leak class.** |
| 2026-05-09_ship_pollution_and_phase1_followups.md | **Highly-visible follow-up index** after the 2026-05-09 Hacker News Phase 1 ship cycle. Captures the working-tree-pollution problem with `/ship` from `/opt/universal_agent` (production checkout being used as agent scratch directory — accumulates 245+ untracked files that get swept into auto-commits), three options to fix it (A: habit change → `/ship` from `~/dev/universal_agent`, B: workflow-PR safety gate, C: `.gitignore` audit + janitor cron), two pre-existing real runtime bugs surfaced as issues #176 (`should_mirror` NameError) and #177 (`resolve_opus` missing import), two deferred Phase 1 implementation follow-ups (`page.tsx` 1349-LOC split, `refresh_now` synchronous handler timing out at the 30s gateway proxy boundary), and a verification checklist for tomorrow morning to confirm the cron is producing snapshot data. **Read first before re-running `/ship` from this checkout.** |

## Proactive Signals (proactive_signals/)

| Doc | Subject |
|-----|---------|
| generation_rules.md | Rules governing proactive signal generation |
| claudedevs_intel_v2_design.md | **Canonical v2 design** — full ClaudeDevs X Intel v2 architecture: vault as canonical product, append-dominant Memex maintenance, Phase 0 dependency currency, Phase 1 deep doc absorption + research grounding, Phase 2/4 Simone orchestration with multi-loop iteration, Phase 3 Cody implementation in vanilla Anthropic-native environment, Phase 5 skill memorialization, parallel-vault backfill plan, generalization to Codex/Gemini lanes, 13-PR implementation sequence. **2026-05-16 addendum:** § 20 documents the 2026-05-09 flyout flip (auto-queue removed; operator drawer is the only producer path) and the 2026-05-16 follow-ups (briefing surfacing + vault-attach heartbeat directive). |
| claudedevs_intel_v2_remaining_work.md | **Living execution plan** — reconciled view of what's shipped and what's left. Cross-references every PR back to the original design § 16. Maps the § 17 deferred decisions to the PRs that resolve them. Tracks new PRs surfaced during execution. **2026-05-16 update:** PR 8 producer marked superseded by `5a3a936a` operator-gated drawer + 2026-05-16 briefing surfacing; PR 10 Phase 4 vault-attach now wired into the Simone heartbeat directive with a one-shot legacy-demo backfill script. PR 14 scope expanded to a four-PR YouTube unification initiative — see `youtube_demo_unification_plan.md`. |
| youtube_demo_unification_plan.md | **Deferred next-phase plan** (2026-05-16) — captures the initiative to flow YouTube tutorial "runnable demo" creation through the same CSI demo workspace pattern (BRIEF/ACCEPTANCE/manifest + `cody-work-evaluator` + iteration + `vault-demo-attach`). Keeps existing `youtube-tutorial-creation` skill for the standalone `CONCEPT.md` half. Sub-PRs 14a (generalize `provision_demo_workspace`), 14b (`youtube_demo_request` Task Hub plumbing), 14c (producer wiring), 14d (optional vault). ~560 lines total. Trigger condition: ≥5 organic CSI tier-3 end-to-end runs first. Operator agreed to defer until CSI v2 is live-verified. |
| csi_v3_backfill_restart_handoff_2026-05-08.md | **Self-contained handoff** for a new conversation that picks up the V2/V3 Twitter intelligence backfill work after the 2026-05-08 session. V3 = V2 architecture + a self-imposed rate-limiting layer (`csi_llm_pacing.py`) added today after Z.AI sustained-burst slowdowns. Captures: what's deployed on main vs sitting in `/tmp/ua-wt-csi-pacing/` worktree (4 files, 15 passing unit tests, NOT merged); the throttling root cause (US night = China business-day peak); the **known-unresolved logging bug** where pacing telemetry is invisible in actual subprocess output despite working in standalone tests; three options for the new session (stderr-print fallback, investigate logger plumbing, or skip pacing and rely on schedule shift); a concrete env-var pacing config; the recommended launch window (US 12:00–17:00 CDT = China deep-night off-peak); and a 9-step concrete next-session checklist. Pasteable as the opening prompt of a new clean conversation. |
| intel_lane_templates.md | **Operator guide** — step-by-step recipe for adding a new intel lane (Codex, Gemini, or any other). Walks through YAML config, vault provisioning, Infisical secrets, manual run, cron registration, dashboard. Documents what's already lane-aware (research grounding, ClaudeCodeIntelConfig, demo_workspace endpoint_profile) vs what still hard-codes the canonical lane. Verification checklist + general recipe + Codex/Gemini-specific differences. |
| insight_pipeline_consolidation_spec.md | **Approved design** (2026-05-28) — Consolidates the duplicated proactive-insight pipeline. Three problems addressed: double email delivery (Atlas direct-send + cron digest), CSI redetecting same convergence repeatedly (unstable `sha256(primary_topic \| sorted(video_ids))` identity), threshold judgment in the wrong place (CSI cron's Track A/B LLM gating before Atlas sees candidates). Architecture: CSI cron becomes pure SQL pattern detection at 3x/day; Atlas owns ship/skip/defer + authoring via new `/evaluate-and-author-intel-brief` skill (batch mission, serial-within-mission, reads `recent_briefs_index.md` for prior-verdict awareness, pre-signs feedback URLs at artifact-write time); Simone owns hourly digest via new `/hourly-intel-digest` skill triggered every heartbeat with hour-bucket self-throttle; operator thumbs up/down writes `operator_rating` via signed-URL GET endpoint, feeds back into Atlas's index. Re-evaluation is evidence-driven (new `video_ids` → new candidate hash) not time-driven. Spec includes Mermaid dispatch flow, full component inventory with code citations, schema changes (`convergence_candidates` table, `verdict`/`verdict_reasoning` columns on `proactive_artifacts`), email template HTML structure with gold/silver/bronze visual hierarchy, failure-mode catalog, and PR-A-through-E migration plan (tier-1 dial flips → index helpers + endpoints → Atlas skill → Simone digest skill → cleanup). |
| insight_pipeline_remediation_plan_2026-05-28.md | **Plan for review** (2026-05-28) — Post-deploy diagnosis of why the consolidated hourly digest never fires and why Atlas still sends per-insight `[VP Status]` emails after PRs A–D landed (deployed SHA `6e03d331`). Code + live-DB verified on the VPS. Findings: (1) the new `convergence_candidate` generation path **works** (9 candidates written by the 00:00 UTC CSI cron, correctly routed with `metadata.invoke_skill` + `preferred_vp`, score 8.6) but the delegated Atlas missions **never execute** `/evaluate-and-author-intel-brief` — `proactive_artifacts` has **0** rows with `artifact_type='intel_brief'` and **0** with any `verdict`, so the digest's `verdict='ship' AND artifact_type='intel_brief'` query matches nothing and exits `no_candidates` every heartbeat; (2) the legacy generators were never decommissioned (PR E deferred) — a **729-item** backlog of old `insight-brief`/`insight_detection` tasks is being drained via the legacy direct-email behavior, producing **92 failure_diagnostics in 6h** and consuming Atlas throughput; (3) PR B's migration is partial — `verdict` added but `delivery_channel` missing, which would break `is_throttled()`; (4) no explicit `convergence_candidate` routing rule in `todo_dispatch_service.py`. Explicitly NOT an AgentMail 429 / GWS-COI problem (delivery is healthy). Five-phase remediation (confirm keystone → quarantine legacy backlog + emails → make candidates execute end-to-end → finish `delivery_channel` migration → verify digest fires → PR E deletion) with Mermaid flowchart + sequence diagram, live-DB evidence table, file:line citations, and a files-in-scope table. |
| llm_convergence_clustering_2026-05-29.md | **Implemented** (2026-05-29) — fixes the upstream cluster-quality failure surfaced by live verification (Atlas correctly skipped 100% of candidates because `_detect_clusters_sql` bucketed by a coarse `primary_topic` string, lumping cooking/crypto/true-crime under "ai_coding"). Restores LLM judgment as a precision layer over the cheap SQL recall net: `_refine_cluster_with_llm` makes one bounded ZAI/GLM call per bucket (via `llm_classifier._call_llm`), returns the genuinely-converging subset + a one-line thesis + 1-10 `signal_strength`, and emits a candidate only when `is_convergence` AND `signal_strength ≥ UA_CONVERGENCE_MIN_STRENGTH` (default 7) AND ≥2 independent channels — fails closed on any LLM/parse error. Thesis is persisted to `convergence_candidates.metadata` and injected into the Atlas task description. CSI cron cadence restored to hourly active-window (`UA_CSI_CONVERGENCE_CRON_EXPR` default `0 6-21 * * *`) and detection no longer gated on `upserted>0`, since ZAI quota is abundant (operator decision). Reverses PR C's "no LLM in detection" decision and supersedes its guard tests. Rollback: `UA_CONVERGENCE_LLM_CLUSTERING=0` → legacy SQL string-match. Also collapses candidate volume, relieving the single-VP-worker bottleneck. Mermaid recall→precision flow, config table, file citations; tests in `test_convergence_llm_clustering.py` + updated `test_write_convergence_candidate.py` / `test_proactive_convergence.py`. |
| insight_pipeline_completion_spec_2026-05-29.md | **Spec-driven plan + Phase 0.5 shipped** (2026-05-29) — remaining-work spec (7 phases) to reach reliable hourly digest delivery. Phase 0 live verification proved the pipeline executes/evaluates/dedups correctly but Atlas ships nothing because its preference context is self-poisoned: `implicit_outcome` "Outcome: park" signals (−0.1 each, ~298 → −1.00 on project:proactive/insight_detection/agent-ready) auto-fired on every task park/skip, injected into Atlas, biasing it to skip → more parks → reinforcing doom loop (the flush worsened it). The 2026-05-24 fix scoped the *gate* (`should_block_proactive_task`) to explicit feedback but left the *delegation context* (`get_delegation_context`→`rebuild_preference_snapshot`) aggregating all signals AND mislabeling implicit as "explicit". Phase 0.5 (shipped): `_fire_implicit_preference_signal` no-ops by default (`UA_PROACTIVE_IMPLICIT_SIGNALS_ENABLED=0`); `rebuild_preference_snapshot` scoped to `signal_type='explicit_feedback'`; one-time detox via `preference_signal_detox`. Operator decisions: cadence=active-window, concurrency=2, sensitive-content=accept-drop, implicit=stop-emitting. Secondary finding: even de-poisoned, current CSI source mix yields mostly low-value convergences (Google-I/O saturation, partisan echo) — source/lane tuning is a follow-on. Mermaid phase graph, 9 testable success criteria, risk table; tests in `test_preference_gate_scoping.py` + updated `test_proactive_outcome_tracker.py`. |
| ideation_sweep_2026-05-29.md | **Implemented** (2026-05-29) — restores the higher-value insight engine (Track B). The original pipeline ran two LLM sweeps: Track A (`track_a_concrete_convergence`, same-story convergence) and Track B (`track_b_ideation_synthesis`, non-obvious abstract patterns/trends). The consolidation removed both; PR #552 restored only Track A. Convergence structurally surfaces news saturation (Atlas correctly skips); the insights the operator valued (e.g. "Narrative Warfare as Primary Conflict Theater" — four unrelated stories, identical manufactured-reality format) are Track B outputs. Restored as an hourly sweep: `_run_ideation_sweep[_async]` loads the recent corpus (`_load_recent_signatures`), chunks into 20s (track_b's cap), runs `track_b_ideation_synthesis` via ZAI/GLM, keeps insights ≥`UA_IDEATION_MIN_CONFIDENCE` (0.7) with ≥2 supporting videos, fails closed per batch. Routes through the SAME de-poisoned `convergence_candidate → Atlas → digest` path via `write_convergence_candidate(candidate_kind='ideation', thesis=narrative, value=value)`; `_candidate_task_description` renders an ideation-specific framing; the Atlas skill's rubric branches on `candidate_kind` (ideation judged on novelty/insight/actionability, NOT same-story overlap). Safe now that implicit-signal poison is gone (PR #553) — skipped ideation candidates no longer poison preference. Config: `UA_IDEATION_SWEEP_ENABLED` (default on), `UA_IDEATION_MIN_CONFIDENCE` (0.7). Cadence inherits the CSI cron. Mermaid dual-sweep flow, config table; tests in `test_ideation_sweep.py` + updated `test_write_convergence_candidate.py`. |

## Handoffs

| Doc | Subject |
|-----|---------|
| coding_handoff.md | Code-verified implementation handoff for the run-per-task workspace refactor across tracked chat, trusted email, dispatcher claims, run/attempt lineage, Session Explorer resolution, acceptance criteria, and validation steps |

## Integrations (integrations/)

| Doc | Subject |
|-----|---------|
| hackernews_phase1_plan.md | **Phase 1 implementation plan** for the Hacker News CLI integration — standalone `/dashboard/hackernews` tab fed by half-hourly `hackernews-pp-cli` cron. Pre-built binary install, XDG-pinned SQLite store, snapshot JSON with 48-deep ring buffer, FastAPI router, single sidebar entry, 8 panels per the design prototype. No LLM, no client-side polling, no integration with Task Hub / CSI / Simone in Phase 1 (Phase 2 catalogued). Approved 2026-05-09 via /grill-me. |
| hackernews_phase2_plan.md | **Phase 2 implementation plan**, approved 2026-05-09 via `/grill-me`. Two leveraged integrations from the Phase 1 catalog. **Lane A:** Pulse → Simone briefing context. Hybrid γ″ architecture — helper does deterministic HN-internal fetches via the `hackernews-pp-cli` (comment threads + post bodies + Algolia per-watchlist-topic comment search); Atlas selectively fetches article bodies via the ZAI-native `webReader` MCP during its briefing mission, with `webSearchPrime` available for follow-up research. The existing briefing LLM does the synthesis per CLAUDE.md's LLM-Native rule. **Lane B:** Movers → CSI lane — wires `build_snapshot()` to emit `hackernews_movers_signal` events into the existing `/var/lib/universal-agent/csi/csi.db` events bus with materiality filter (`status=new`, `delta≥3`, high-score drops, top-3 controversial) and 24h dedup. ~860 LOC product+test, ~80 LOC docs. Six-phase rollout (P2.A1→A3, P2.B1→B3). Adds `web-reader` and `web-search-prime` ZAI MCPs to `.mcp.json` as part of the Phase 2 deploy and introduces the new "Tool Surface by Execution Mode" matrix in `10_Interactive_Coding_Environment.md` to formalize when ZAI MCPs vs Anthropic-native tools are appropriate (autonomous mode = ZAI; interactive/demo modes = Anthropic). |

## Remaining Operational References (03_Operations/)

| Doc | Subject |
|-----|---------|
| 01 | Heartbeat Debug Fixes — historical debug reference |
| 02 | Browser Debugging Lessons — debugging patterns, browser-profile comparison, and targeted storage-key resets |
| 11 | Scheduling Runtime V2 Operational Runbook — cron operations, timeout handling, and stuck-run crash reports |
| 13 | Skill Dependency Setup Guide — skill installation |
| 14 | Session Runtime Behavior And Recovery Model |
| 15 | Execution Lock Concurrency Architecture |
| 16 | Concurrency Conflict Root Cause — VP general interim path |
| 19 | VPS App/API/Telegram Deployment Explainer |
| 20 | VPS Daily Ops Quickstart |
| 22 | VPS Remote Dev, Deploy And File Transfer Runbook |
| 23 | Agent Workspace Inspector Skill |
| 24 | VPS Service Recovery System Runbook |
| 26 | VPS Host Security Hardening Runbook |
| 27 | Deployment Runbook |
| 30 | Memory System Architecture And Health |
| 31 | VPS Deployment Decision Tree |
| 32 | VPS FileBrowser Setup And Access |
| 76 | Sandbox Permissioning And Exception Profile |
| 78 | Daily Autonomous Briefing Reliability |
| 79 | Golden Run Research Report Pipeline Reference |
| 80 | Google Workspace Integration Retrospective Memo |
| 99 | **Documentation Drift Maintenance Pipeline** — canonical source-of-truth |
| 103 | Debugging Lessons Living Document — reusable debugging lessons from complex production incidents, including the April 5 dashboard browser-state incident and SHA-first deploy verification |
| 106 | TaskStop Guardrails and Task Hub Execution Hardening — explainer for the March 31 hardening work: SDK task control versus Task Hub lifecycle, run-aware `TaskStop` blocking, corrective guidance, and the resulting pipeline reliability improvements. _Updated 2026-05-29: added § 8.1 deploy-restart casualty suppression — `execution_missing_lifecycle_mutation` is downgraded to a dashboard-only warning during a deploy window so self-healing restart kills don't email the operator._ |

## Cleanup Summary

**72 outdated documents** were deleted on 2026-03-06:
- 6 stale architecture docs (01_Architecture/) — replaced by 3 new docs written from source code
- 46 implementation plans, verifications, assessments, and handoffs for deployed systems
- 20 superseded operational docs covered by canonical source-of-truth documents
