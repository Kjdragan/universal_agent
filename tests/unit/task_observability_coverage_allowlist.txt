# Hermes Ship 5 — Task Hub Observability Protocol allowlist.
#
# Pre-existing subprocess-spawning files in src/universal_agent/ that
# predate the protocol shipped in Ship 4. Each entry MUST justify
# why the file is on the list. Two acceptable rationales:
#
#   (a) "Not a task unit" — the spawn is infrastructure (CLI launcher,
#       Playwright runtime, PDF extractor, OAuth bridge, GWS calendar
#       fetch, dependency sweep). The work it performs is not a
#       Task Hub entity; there is no task_hub_items row to wire to.
#       The protocol doesn't apply.
#
#   (b) "Legacy task unit, follow-up wiring planned" — the spawn IS
#       part of a task lifecycle and should eventually be wired. The
#       entry stays here until a follow-up PR migrates it.
#
# Adding a NEW entry to this allowlist requires explicit operator
# approval. The protocol is the long-term standard; allowlist exists
# only to make adoption incremental, not to permit drift.
#
# Removing an entry happens automatically when the file becomes
# compliant (imports a protocol helper). The lint guard warns on
# stale entries but doesn't fail — operator cleans them up in a
# follow-up PR.
#
# See docs/03_Operations/129_Task_Hub_Observability_Protocol.md
# for the rules this allowlist gates.

# ── (a) Not a task unit — infrastructure subprocess ───────────────────

src/universal_agent/agent_college/professor.py
src/universal_agent/api/server.py
src/universal_agent/delegation/system_handlers.py
src/universal_agent/gateway_server.py
src/universal_agent/notebooklm_runtime.py
src/universal_agent/scripts/dependency_currency_sweep.py
src/universal_agent/scripts/youtube_provision_digest_playlists.py
src/universal_agent/services/agentmail_service.py
src/universal_agent/services/csi_url_judge.py
src/universal_agent/services/dag_handlers.py
src/universal_agent/services/dependency_upgrade.py
src/universal_agent/services/gws_calendar_context.py
src/universal_agent/services/gws_event_listener.py
src/universal_agent/services/hackernews_snapshot_service.py
src/universal_agent/services/invariants/zai_inference_health.py
src/universal_agent/templates/_smoke_demo/smoke.py
src/universal_agent/tools/link_bridge.py
src/universal_agent/tools/pdf_bridge.py

# ── (b) Legacy task units — follow-up wiring planned ─────────────────

src/universal_agent/vp/autonomous_mission_executor.py
src/universal_agent/vp/worker_loop.py
src/universal_agent/vp/worker_main.py
src/universal_agent/vp/worktree_utils.py
src/universal_agent/worker.py

# ci_failure_grace_recheck.py: the only subprocess use is ephemeral read-only
# `gh` CLI calls (GitHub issue/PR/run state re-verification + issue label edits)
# at grace-fire time. It is NOT a Task-Hub worker spawning a tracked unit of work,
# so the worker-exit/PID observability protocol does not apply. (ci-autofix hook, 2026-05-30)
src/universal_agent/scripts/ci_failure_grace_recheck.py

# scratch_publish.py [rationale (a) — not a task unit]: the only subprocess use is a
# single short-lived, synchronous shell-out to `scripts/publish_scratch.sh` (publish
# one HTML file to the tailnet scratchpad, capture the printed URL, return). There is
# no long-running PID to classify or persist and no task_hub_items row — it is infra,
# not a tracked unit of work — so the worker-exit/PID protocol does not apply.
# (scratchpad delivery, 2026-06-02)
src/universal_agent/services/scratch_publish.py

# prune_scratch.py [rationale (a) — not a task unit]: the daily scratchpad GC sweep
# (filesystem GC + best-effort index rebuild). Its ONLY subprocess use is a single
# short-lived, synchronous `subprocess.run([sys.executable, build_scratch_index.py, …])`
# (regenerate the browsable artifact index after a prune, capture_output, check=False,
# timeout=60, best-effort). There is no long-running PID to classify and no
# task_hub_items row — it is a filesystem-GC/index-rebuild shell-out, not a tracked
# unit of work — so the worker-exit/PID protocol does not apply. Mirrors its sibling
# scratch_publish.py. (scratchpad docset-viewer, 2026-06-16)
src/universal_agent/scripts/prune_scratch.py

# skill_gap_finder.py [rationale (a) — not a task unit]: a DESKTOP-only weekly cron
# (user systemd timer, not a gateway/VPS worker). Its only subprocess use is ephemeral
# read-only `gh` CLI (issue dedup `gh issue list` + `gh issue create` for the weekly
# skill-gap report). No long-running PID to classify, no task_hub_items row — it is
# infra, not a tracked Task Hub unit of work — so the worker-exit/PID protocol does not
# apply. (technical-skill-finder proactive integration, 2026-06-07)
src/universal_agent/skill_gap_finder.py

# backlog_triage.py [rationale (a) — not a task unit]: a daily backlog-triage cron whose
# only subprocess use is ephemeral read-only `gh` CLI (issue/PR list to assess the open
# backlog). It composes a Simone email + seeds ONE reminder row; no long-running PID to
# classify and no task_hub_items row — it is infra, not a tracked unit of work — so the
# worker-exit/PID protocol does not apply. (backlog-triage proactive integration, 2026-06-07)
src/universal_agent/backlog_triage.py

# deslop_remediation_dispatch.py [rationale (a) — not a task unit]: the deslop
# auto-remediation poller (Theme 2 v1a). Its only subprocess use is ephemeral
# read-only/label-edit `gh` CLI (list open deslop-findings issues, view bodies,
# add the deslop-dispatched/needs-operator claim labels) at poll time. It queues
# a Cody mission via queue_proactive_task (a task_hub row owned by the Cody
# worker, not by this poller) and otherwise spawns no long-running PID — it is
# infra, not a tracked unit of work — so the worker-exit/PID protocol does not
# apply. Mirrors ci_failure_grace_recheck.py. (auto-remediation dispatcher v1, 2026-06-07)
src/universal_agent/scripts/deslop_remediation_dispatch.py

# zai_activity_control.py [rationale (a) — not a task unit]: the ZAI proactive-activity
# control plane behind the ops dashboard. Its ONLY subprocess use is ephemeral
# `systemctl show` (read) and `sudo -n systemctl <start|stop|…> <allowlisted-unit>`
# (operational service control) — argv-only, allowlist-validated. These are systemd
# control shell-outs, not a task-executing worker: no long-running PID to classify and
# no task_hub_items row to own — it is operational infra, not a tracked unit of work — so
# the worker-exit/PID protocol does not apply. Mirrors the factory local-service-control
# pattern. (ZAI activity controls, 2026-06-11)
src/universal_agent/services/zai_activity_control.py

# nightly_wiki_agent.py [rationale (a) — not a task unit]: its ONLY subprocess use is a
# single ephemeral read-only `nlm notebook list --json` at dispatch time, to count today's
# wiki notebooks for the project-wide daily hard cap (the 2026-06-14 runaway fix). No
# long-running PID to classify and no task_hub_items row — it is a pre-flight count, not a
# tracked unit of work — so the worker-exit/PID protocol does not apply. The actual wiki work
# runs in the dispatched VP mission (already protocol-wired), not in this script.
# (proactive-lane runaway protection, 2026-06-14)
src/universal_agent/scripts/nightly_wiki_agent.py

# proactive_activity_report.py [rationale (a) — not a task unit]: builds the proactive
# report's activity inventory. Its ONLY subprocess use is a single ephemeral read-only
# `systemctl list-timers --all --no-pager` to read systemd-timer last/next run times for
# health classification — argv-only, no shell, no input. No long-running PID to classify
# and no task_hub_items row to own — it is a diagnostic read, not a task-executing worker —
# so the worker-exit/PID protocol does not apply. Mirrors the zai_activity_control.py
# `systemctl show` read pattern. (proactive activity report, 2026-06-21)
src/universal_agent/services/proactive_activity_report.py

# disk_usage_health.py [rationale (a) — not a task unit]: the watchdog invariant that
# reports which directories are filling the disk. Its ONLY subprocess use is an ephemeral
# read-only `du -x --max-depth=1` to measure directory sizes — argv-only, no shell, no
# input, minimal env (PATH/LC_ALL only, so Infisical secrets are never handed to it). It
# measures the filesystem; it does not execute a unit of work: there is no task_hub_items
# row to own and no long-running worker PID to classify (the call is bounded and its
# result is a number). Same shape as the proactive_activity_report.py `systemctl
# list-timers` read directly above. It replaced a hand-rolled Python byte-walk that
# silently truncated and reported partial sums as real sizes — `du` is the correct
# primitive, not a worker spawn. (disk watchdog accuracy fix, 2026-07-15)
src/universal_agent/services/invariants/disk_usage_health.py

# vp_coder_workspace_pruner.py [rationale (a) — not a task unit]: the weekly
# workspace-pruning maintenance sweep. It became a spawn site on 2026-07-24 when the
# merged-worktree prune tier was added; every one of its subprocess calls is ephemeral
# and bounded — read-only git interrogation (`git worktree list --porcelain`,
# `status --porcelain`, `rev-parse`, `rev-list --count`, `merge-base --is-ancestor`),
# one `git fetch <remote> <branch>`, `git worktree prune`/`remove`, and a read-only
# `gh pr list --state merged` (argv-only, no shell, explicit timeout, minimal env
# allow-list so Infisical secrets are never handed to `gh`). It reclaims disk; it does
# not execute a unit of work: there is no task_hub_items row to own and no long-running
# worker PID to classify. Same shape as prune_scratch.py (filesystem GC sweep) and
# disk_usage_health.py (`du` measurement) above. (merged-worktree auto-prune, 2026-07-24)
src/universal_agent/scripts/vp_coder_workspace_pruner.py

# finalize_failure_context.py [rationale (a) — not a task unit]: the VP-coder
# finalize-crash diagnosability module. Its ONLY subprocess use (`_run_git`) is
# two ephemeral, bounded (timeout=5), argv-only, READ-ONLY calls —
# `git rev-parse HEAD` and `git status --porcelain` — used to build a pre-crash
# WorkSnapshot for the mission-outcome payload. It spawns no long-running
# worker PID and owns no task_hub_items row, so there is no exit status for
# worker_exit_classifier to classify and no task row for the PID protocol to
# attach to: it is a forensic git read, not a tracked unit of work. Same shape
# as vp_coder_workspace_pruner.py and prune_scratch.py above.
# (vp-finalize-diagnosability, allowlisted 2026-07-31)
src/universal_agent/vp/finalize_failure_context.py

# proactive_pipeline_invariants.py [rationale (a) — not a task unit]: the
# Infisical CLI auth-expiry invariant. Its subprocess use is a single bounded
# (timeout), argv-only, check=False probe — `infisical secrets get <PROBE_KEY>
# --env <env> --plain` — against a deliberately NONEXISTENT key, so it
# exercises the CLI's auth path and can never return a real secret value. It
# spawns no long-running worker PID and owns no task_hub_items row: there is
# no exit status for worker_exit_classifier to interpret and nothing for the
# PID protocol to attach to. Same shape and rationale as
# finalize_failure_context.py and prune_scratch.py above.
# (infisical-cli-auth-invariant, allowlisted 2026-07-31)
src/universal_agent/services/invariants/proactive_pipeline_invariants.py
