# Discord Intelligence Subsystem - Requirements
# Part of Universal Agent

# Standard discord.py for the bot-token Command & Control server
# Provides discord.ui (buttons, modals, views), app_commands (slash commands),
# discord.Intents, and all interactive bot features.
discord.py>=2.4.0

# NOTE: The Intelligence Daemon (daemon.py) uses user tokens and requires
# discord.py-self (a fork that allows user token auth). Since discord.py and
# discord.py-self conflict (both install to the 'discord' namespace), the daemon
# runs in a SEPARATE virtualenv on the VPS via its own systemd service
# (ua-discord-intelligence.service).
#
# To set up the daemon venv:
#   python -m venv /opt/discord-daemon-venv
#   /opt/discord-daemon-venv/bin/pip install discord.py-self>=2.1.0
