---
name: <TASK_NAME>
description: <One-line description of what this task accomplishes>
---

# <Task Title>

## Goal

<What needs to happen — describe the OUTCOME, not the process.
The executing agent will figure out HOW. You just define WHAT "done" looks like.>

## Success Criteria

<Concrete, verifiable conditions. The executing agent checks these after completion.
If all pass, the task is done. If any fail, they become input for the next iteration.>

- [ ] Criterion 1: <specific, measurable>
- [ ] Criterion 2: <specific, measurable>
- [ ] Criterion 3: <specific, measurable>

## Constraints

<Hard limits — things that must NOT happen. These are guardrails, not process steps.>

- Do NOT: <thing that would cause harm>
- Do NOT: <thing that would violate a requirement>
- Budget/scope: <any resource limits>

## Context

<Pointers to help the executing agent orient quickly. Not exhaustive documentation —
just enough to avoid obvious wrong turns.>

- Key file: `<path/to/relevant/file>`
- Related system: <brief description>
- Existing skill to leverage: `.claude/skills/<relevant-skill>/SKILL.md`
- Known gotcha: <something non-obvious>

## Anti-Patterns

<Things the executing agent should avoid. Initially empty for v0 — populated after
failed attempts. Each anti-pattern is a lesson learned.>

<!-- Add anti-patterns here as you learn from execution attempts:
- Don't do X because Y (learned from run #N)
-->
