---
name: <TASK_NAME>
description: <One-line description — more refined than v0 based on observed execution>
---

# <Task Title>

## Goal

<Refined from v0 based on what you learned during execution.
May be more specific or differently scoped than the original.>

## Success Criteria

- [ ] Criterion 1: <refined based on what actually matters>
- [ ] Criterion 2: <added based on edge cases discovered>
- [ ] Criterion 3: <kept from v0 if still relevant>

## Constraints

- Do NOT: <original constraints>
- Do NOT: <new constraints discovered during execution>

## Context

- Key file: `<path/to/relevant/file>`
- Related system: <description>
- Existing skill: `.claude/skills/<skill>/SKILL.md`

## Approach (Observed)

<What actually worked when this task was executed. Extracted from the agent's
successful run — not pre-planned, but observed and documented.>

### What Worked
1. <Step the agent took that was effective>
2. <Another effective approach>
3. <Key decision point and why this path was chosen>

### What Didn't Work
1. <Approach that failed and why>
2. <Dead end that wasted time>

## Anti-Patterns (Learned)

<Populated from actual execution failures. Each one prevents a future agent
from repeating the same mistake.>

- Don't <X> because <Y> — learned from run #1
- Avoid <approach> because <consequence> — learned from run #2

## Scripts (Extracted)

<If the executing agent wrote utility scripts that worked well, extract them
here so future runs don't reinvent them.>

<!-- Example:
See `scripts/setup_env.py` — handles the environment configuration that
every run needs. Run with: `python scripts/setup_env.py --config <path>`
-->

## References

<Domain knowledge files added based on what the executing agent needed.
Only include if the agent had to discover this information the hard way.>

<!-- Example:
See `references/api_schema.md` — the API schema that's not documented
anywhere obvious but is critical for the integration step.
-->
